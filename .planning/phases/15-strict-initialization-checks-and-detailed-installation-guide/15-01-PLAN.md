---
phase: 15-strict-initialization-checks-and-detailed-installation-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/tool-validator.js
autonomous: true
requirements:
  - D-15-10
  - D-15-11
  - D-15-12
  - D-15-15
tags:
  - cli
  - init
  - validator
  - instructions-db
must_haves:
  truths:
    - "INSTRUCTIONS_DB records expose purpose, whyRequired, detectionHint, checkedPaths, installCommands, workspaceFallback, homeFallback, verifyCommand fields for every required tool"
    - "Existing callers of getToolInstructions (handleDoctor, checkStagePrerequisites) continue to work without code change because description and instructions fields remain present"
    - "Records contain no network-resolution logic and reference only known package managers / copy commands"
  artifacts:
    - path: "lib/tool-validator.js"
      provides: "Enriched INSTRUCTIONS_DB with 8-field structured records"
      contains: "purpose"
  key_links:
    - from: "lib/tool-validator.js"
      to: "INSTRUCTIONS_DB.gsd"
      via: "object literal extension"
      pattern: "whyRequired"
---

<objective>
Enrich the structured `INSTRUCTIONS_DB` in `lib/tool-validator.js` from a 2-field record (`description`, `instructions`) to an 8-field structured record per D-15-10 / D-15-11. The new fields power the terminal and Markdown formatters delivered in Plan 02. The change is **additive** — existing fields stay so that current callers (`handleDoctor`, `checkStagePrerequisites` in `lib/flow-engine.js`) continue to work without modification.

Purpose: Centralize all installation guidance into one structured DB so the CLI and the repair guide never duplicate prose (D-15-11). Keep the database local/offline (D-15-12).
Output: An updated `INSTRUCTIONS_DB` with enriched records for every required tool, plus a smoke unit confirming `getToolInstructions` returns the new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-CONTEXT.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-PATTERNS.md
@lib/tool-validator.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend INSTRUCTIONS_DB records with 8-field structured shape</name>
  <files>lib/tool-validator.js</files>
  <read_first>
    - lib/tool-validator.js (entire file — current `INSTRUCTIONS_DB` lives at lines ~88-105; `getToolInstructions` at ~113-122; module.exports at ~124-128)
    - .planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md (§"Example A — Enriched INSTRUCTIONS_DB record" for canonical 8-field shape; §"Required-vs-Optional Check Inventory" for the required tool set)
    - .planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-PATTERNS.md (§"lib/tool-validator.js (MODIFY — enrich INSTRUCTIONS_DB)" for the back-compat rule)
  </read_first>
  <action>
    For every tool record currently in `INSTRUCTIONS_DB` (at minimum: `gsd`, `superpowers`, `spec-kit`, `gstack` — confirm complete set when reading file), preserve the existing `description` and `instructions` fields unchanged, then add these new fields per D-15-10:
    - `purpose` (string, one sentence)
    - `whyRequired` (string, one sentence explaining which stage / capability needs it)
    - `detectionHint` (string, names the skill folder or PATH command)
    - `checkedPaths` (string[] — workspace-local, then `.claude/skills/`, then `~/.gemini/config/skills/`)
    - `installCommands` (string[] — bash commands, may include `#` comment headers; MUST NOT reference any URL fetch / curl / network operation per D-15-12)
    - `workspaceFallback` (string, one sentence)
    - `homeFallback` (string, one sentence)
    - `verifyCommand` (string — defaults to `adp doctor`)
    DO NOT modify `getToolInstructions` lookup function (it returns the whole record; adding fields requires no change). DO NOT remove `description` or `instructions` — `handleDoctor` lines 683-684 and `flow-engine.js` lines 332-335 still read them. DO NOT introduce any new module imports. Keep CommonJS, 2-space indent, semicolons per project convention. Surface text MUST keep literal `~` (not resolved absolute home paths) per Security pitfall in RESEARCH.md §"Known Threat Patterns".
  </action>
  <verify>
    <automated>node -e "const v=require('./lib/tool-validator'); const r=v.getToolInstructions('gsd'); ['purpose','whyRequired','detectionHint','checkedPaths','installCommands','workspaceFallback','homeFallback','verifyCommand','description','instructions'].forEach(k=>{ if(!(k in r)) throw new Error('missing '+k); }); if(!Array.isArray(r.checkedPaths)) throw new Error('checkedPaths not array'); if(!Array.isArray(r.installCommands)) throw new Error('installCommands not array'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "whyRequired" lib/tool-validator.js` returns at least 4 lines (one per tool record).
    - Source assertion: `grep -n "description:" lib/tool-validator.js` still returns at least 4 lines (back-compat preserved).
    - Behavior assertion: `node -e "console.log(JSON.stringify(require('./lib/tool-validator').getToolInstructions('gsd'),null,2))"` prints an object containing both `description` and `purpose` keys.
    - Behavior assertion: `node -e "const r=require('./lib/tool-validator').getToolInstructions('superpowers'); if(r.installCommands.some(c=>/curl|wget|http/.test(c))) throw new Error('network ref'); console.log('OK')"` exits 0 (no network refs per D-15-12).
    - Test command: `npm run validate` exits 0.
  </acceptance_criteria>
  <done>
    All four tool records in `INSTRUCTIONS_DB` carry the 8 new fields plus the original 2 fields. Existing callers (`handleDoctor`, `checkStagePrerequisites`) require no code change. No new dependencies introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → INSTRUCTIONS_DB record | The DB is consulted to render repair-guide Markdown that is then written under `.ai/state/`. Untrusted user paths must never reach this DB; only the static literal record contents do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Information Disclosure | INSTRUCTIONS_DB rendered into committed repair guide | mitigate | Keep literal `~` strings in `checkedPaths` and fallback prose; never resolve absolute home directory paths into static records (per RESEARCH.md §Known Threat Patterns). |
| T-15-01-02 | Tampering | `installCommands` field rendered into Markdown for user copy/paste | mitigate | All command strings are static literals committed to git. No template substitution from user input. Reviewer must confirm commands do not invoke network ops (D-15-12). |
| T-15-01-SC | Tampering | npm / supply chain | accept | No new packages added; project has no `package.json` for CLI. Slopcheck N/A per RESEARCH.md §"Package Legitimacy Audit". |

</threat_model>

<verification>
- `npm run validate` exits 0.
- `node -e "['gsd','superpowers','spec-kit','gstack'].forEach(t=>{const r=require('./lib/tool-validator').getToolInstructions(t); if(!r.purpose||!r.whyRequired||!r.verifyCommand) throw new Error('missing field on '+t);}); console.log('OK')"` exits 0.
</verification>

<success_criteria>
- INSTRUCTIONS_DB records are 8-field-enriched and back-compatible.
- No new external dependencies introduced.
- No `getToolInstructions` callers required code changes.
- `npm run validate` passes.
</success_criteria>

<output>
Create `.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-01-SUMMARY.md` when done.
</output>
