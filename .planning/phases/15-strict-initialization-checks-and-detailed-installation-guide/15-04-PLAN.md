---
phase: 15-strict-initialization-checks-and-detailed-installation-guide
plan: 04
type: execute
wave: 4
depends_on:
  - 15-03
files_modified:
  - validators/scripts/test-cli.js
autonomous: true
requirements:
  - D-15-02
  - D-15-05
  - D-15-08
  - D-15-13
  - D-15-14
  - D-15-15
  - D-15-16
must_haves:
  truths:
    - "Greenfield init produces no `.ai/state/repair-guide.md` and exits 0"
    - "Missing required prerequisite produces exit 1, a repair guide naming the tool, and the guide includes its `verifyCommand`"
    - "Localized SKILL.md with `~/`, `$HOME`, or `.gemini/` in an `@`-prefixed line inside `<execution_context>` produces exit 1 and a repair guide naming the offending file"
    - "Pre-existing `CLAUDE.md` lacking the `## Subagent & Parallel Execution Guidelines` heading produces exit 1 and the repair guide reports `instruction file incomplete` (NOT `tool missing`) per D-15-15"
  artifacts:
    - path: "validators/scripts/test-cli.js"
      provides: "4 new sandbox-based strict-gate tests appended to the existing harness"
      contains: "CLI Init Strict Gate"
  key_links:
    - from: "validators/scripts/test-cli.js"
      to: "bin/adp.js"
      via: "spawnSync via runCLI helper with PROJECT_ROOT/REPO_ROOT env injection"
      pattern: "runCLI\\(\\['init'\\]\\)|runCLI\\(\\['doctor'\\]\\)"
---

<objective>
Add four sandbox-based CLI integration tests to `validators/scripts/test-cli.js` covering the strict-gate scenarios called out by RESEARCH.md §"Common Pitfalls" and the §"Required-vs-Optional Check Inventory". The tests reuse the existing harness (`setupSandbox`, `runCLI`, `addTest`, `writeFile`, `readFile`, `fileExists`, `cleanupSandbox`) — no harness changes required.

Purpose: Lock in the behavior changes delivered by Plans 01-03 against regression, including the four edge cases that the locked decisions explicitly call out: tool missing, broken localization, instruction-file incomplete, and brownfield happy path.
Output: `validators/scripts/test-cli.js` with 4 new `addTest` blocks appended. All existing tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-CONTEXT.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-PATTERNS.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-03-SUMMARY.md
@validators/scripts/test-cli.js
@bin/adp.js
@lib/init-checks.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add four strict-gate sandbox tests</name>
  <files>validators/scripts/test-cli.js</files>
  <read_first>
    - validators/scripts/test-cli.js (entire file — `setupSandbox` lines 8-19, `runCLI` lines 28-42, `addTest` framework, existing `CLI Init Command` line 93, `CLI Doctor Command` line 144, `CLI Init Creates Flow Infrastructure` line 723; assertion helpers `fileExists`, `readFile`, `writeFile`, `writeJson` lines 44-68)
    - .planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md (§"Example C — Test sandbox extension"; §"Required-vs-Optional Check Inventory"; §"Common Pitfalls" Pitfall 2 for the brownfield-missing-section case)
    - .planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-PATTERNS.md (§"validators/scripts/test-cli.js" for sandbox setup, env injection, and the test scaffold)
  </read_first>
  <behavior>
    - Test 1 — `'CLI Init Strict Gate Greenfield Happy Path'`: setupSandbox; pre-create the four mock skill folders already present in `setupSandbox`; run `runCLI(['init'])`; expect `res.code === 0`; expect `fileExists('.ai/state/repair-guide.md') === false`.
    - Test 2 — `'CLI Init Strict Gate Fails on Missing Prerequisite'`: setupSandbox; delete `.agents/skills/gsd-discuss-phase` after sandbox setup so the prereq check fails; run `runCLI(['init'])`; expect `res.code === 1`; expect `fileExists('.ai/state/repair-guide.md')`; expect guide content includes the literal `gsd` and the verify command `adp doctor`.
    - Test 3 — `'CLI Init Strict Gate Fails on Broken Localized SKILL.md'`: setupSandbox; `writeFile('.agents/skills/gsd-discuss-phase/SKILL.md', '# Test\n<execution_context>\n@~/.gemini/antigravity/workflows/foo.md\n</execution_context>\n')`; run `runCLI(['init'])`; expect `res.code === 1`; expect `fileExists('.ai/state/repair-guide.md')`; expect guide includes the substring `SKILL.md` and `category: localization`.
    - Test 4 — `'CLI Init Strict Gate Reports Instruction Section Missing'`: setupSandbox; `writeFile('CLAUDE.md', '# Custom\nNo subagent heading here.\n')` BEFORE running init (so the brownfield-skip policy preserves it); run `runCLI(['init'])`; expect `res.code === 1`; expect guide includes the literal `Subagent & Parallel Execution Guidelines` and the wording for `category: instruction` (NOT `category: tool`) per D-15-15.
  </behavior>
  <action>
    Append four new `addTest` blocks at the end of `validators/scripts/test-cli.js` (before any final summary/report logic if present). Use the existing harness helpers verbatim — DO NOT modify `setupSandbox`, `runCLI`, or any helper. Each test ends with `cleanupSandbox()`. Use the exact test names from the `<behavior>` section so the harness output names match the must_haves.

    Implementation notes per test:
    - Test 1: relies on default `setupSandbox` pre-creating the four required skill folders. After `runCLI(['init'])`, assert exit 0. Then re-read `.ai/state/repair-guide.md` should not exist OR (if it existed pre-run because of a prior failed test) was deleted by `runAndReport` (Pitfall 5).
    - Test 2: call `fs.rmSync(path.join(testSandboxRoot, '.agents/skills/gsd-discuss-phase'), { recursive: true, force: true })` after `setupSandbox()` and before `runCLI`.
    - Test 3: write the SKILL.md AFTER `setupSandbox()` so the file pre-exists when `localizeGlobalSkills` runs (the brownfield-safe policy skips existing files; the strict gate then re-inspects).
    - Test 4: write `CLAUDE.md` AFTER `setupSandbox()` and BEFORE `runCLI(['init'])`. The brownfield-safe policy in `handleInit` preserves the user's CLAUDE.md content; `appendSubagentGuidelines` may append, so the test must either: (a) write a CLAUDE.md that already has the file size so append is skipped, OR (b) confirm by re-reading after init that the heading is still absent. Use approach (b) for robustness: after init exit, re-read `CLAUDE.md` and assert the heading IS absent (the user kept their custom content), and assert the repair guide contains the EXACT literal `Local workflow files incomplete` (per CEO review F4 wording pin in Plan 02).

    All four tests MUST use `if (...) throw new Error('...')` assertion style matching the existing tests (no `assert` lib — none is available). All four tests MUST end with `cleanupSandbox()`. Keep CommonJS, 2-space indent, semicolons.
  </action>
  <verify>
    <automated>npm run test:cli</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "addTest('CLI Init Strict Gate" validators/scripts/test-cli.js` returns at least `4`.
    - Source assertion: `grep -c "cleanupSandbox" validators/scripts/test-cli.js` increases by at least 4 over the pre-Plan-04 count.
    - Behavior assertion: `npm run test:cli` exits 0 with all 25 (21 original + 4 new) tests passing.
    - Behavior assertion: each new test executes `runCLI(['init'])` exactly once (no test calls `runCLI` more than once per scenario).
    - Behavior assertion: Test 4 reads `.ai/state/repair-guide.md` and matches the EXACT literal `Local workflow files incomplete` (pinned in Plan 02 per CEO review F4); the test additionally asserts the guide does NOT contain `Tool missing — claude` or any `category: tool` entry for the CLAUDE.md case (D-15-15 lock-in).
    - Test command: `npm test` (full validation suite) exits 0.
  </acceptance_criteria>
  <done>
    Four new strict-gate tests are appended to `validators/scripts/test-cli.js`. All tests (both pre-existing and new) pass under `npm run test:cli`. The full `npm test` suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test sandbox dir → host filesystem | The harness already writes only under `testSandboxRoot` and cleans up on each test. No new write paths introduced. |
| spawnSync of `node bin/adp.js` → child process | The harness already runs the CLI in a child process with explicit env (`PROJECT_ROOT`, `REPO_ROOT`); the new tests reuse this contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Tampering | Test pollution across runs | mitigate | Every new test ends with `cleanupSandbox()` (acceptance criteria assertion). Existing harness already removes `testSandboxRoot` between tests. |
| T-15-04-02 | DoS | Slow CLI tests blocking CI | accept | Each test is a single `spawnSync('node bin/adp.js init')` call — comparable to existing test cost (validators/scripts/test-cli.js already runs 21 such tests in CI). |
| T-15-04-03 | Tampering | False-negative test passing for the wrong reason | mitigate | Test 4 explicitly negates `category: tool` to prevent the historical Pitfall 2 false-failure pattern (telling the user to "install CLAUDE" when the file simply lacks a heading). |
| T-15-04-SC | Tampering | Supply chain | accept | No new packages added. |

</threat_model>

<verification>
- `npm run test:cli` exits 0 with all 25 tests passing.
- `npm test` exits 0.
- `grep -c "addTest('CLI Init Strict Gate" validators/scripts/test-cli.js` returns at least 4.
</verification>

<success_criteria>
- Four new tests appended to `validators/scripts/test-cli.js` matching the names in `<behavior>`.
- All four tests pass and all 21 pre-existing tests continue passing.
- Test 4 specifically distinguishes "instruction file incomplete" from "tool missing" (D-15-15 lock-in).
</success_criteria>

<output>
Create `.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-04-SUMMARY.md` when done.
</output>
