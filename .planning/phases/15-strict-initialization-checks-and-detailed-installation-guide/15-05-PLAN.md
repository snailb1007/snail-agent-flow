---
phase: 15-strict-initialization-checks-and-detailed-installation-guide
plan: 05
type: execute
wave: 4
depends_on:
  - 15-02
files_modified:
  - validators/scripts/test-init-checks.js
  - package.json
autonomous: true
requirements:
  - D-15-05
  - D-15-06
  - D-15-08
  - D-15-13
  - D-15-14
  - D-15-15
tags:
  - tests
  - init-checks
  - unit
must_haves:
  truths:
    - "validators/scripts/test-init-checks.js exercises every check ID from RESEARCH.md §Required-vs-Optional Check Inventory directly against runStrictChecks (not via CLI)"
    - "Each check ID has at least one passing and one failing case, asserting both 'passed' boolean and evidence shape"
    - "Pitfall 1 regex behavior is tested: documentation prose containing literal '~' is NOT flagged; @-prefixed lines inside <execution_context> WITH `~`/`$HOME`/`.gemini/` ARE flagged"
    - "FS error rescue (T-15-02-06) is tested: EACCES / ENOENT-mid-scan / EISDIR produce failed CheckResults rather than thrown exceptions"
    - "formatMarkdownGuide output for category=instruction contains the EXACT literal 'Local workflow files incomplete' (D-15-15 wording pin)"
    - "Test runs in under 5 seconds (no spawnSync overhead — pure-function tests against tempdir fixtures)"
  artifacts:
    - path: "validators/scripts/test-init-checks.js"
      provides: "Unit tests for lib/init-checks.js pure module"
      contains: "runStrictChecks"
      min_lines: 200
    - path: "package.json"
      provides: "npm run test:init-checks script + wired into npm test"
      contains: "test:init-checks"
  key_links:
    - from: "validators/scripts/test-init-checks.js"
      to: "lib/init-checks.js"
      via: "direct require — no child process"
      pattern: "require\\('\\.\\./\\.\\./lib/init-checks'\\)"
---

<objective>
Add direct unit tests for `lib/init-checks.js` (the pure module produced by Plan 02). The CLI integration tests in Plan 04 cover end-to-end behavior; this plan adds fast, fine-grained coverage of every check ID, evidence shape, regex behavior, and FS-error rescue path.

Purpose: A pure ~200-line module with 9+ check IDs and the Pitfall-1 regex deserves direct unit coverage (per CEO review F1). Unit tests catch wrong-evidence-shape regressions that CLI smoke tests miss, and run in seconds not seconds-per-spawn.
Output: `validators/scripts/test-init-checks.js` with 12-15 unit tests; `npm run test:init-checks` script; wired into `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-CONTEXT.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-PATTERNS.md
@.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-02-SUMMARY.md
@lib/init-checks.js
@validators/scripts/test-cli.js
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author unit tests for runStrictChecks</name>
  <files>validators/scripts/test-init-checks.js</files>
  <read_first>
    - lib/init-checks.js (study exported runStrictChecks, formatTerminal, formatMarkdownGuide)
    - validators/scripts/test-cli.js (reuse the addTest framework, throw-on-fail style, and tempdir cleanup pattern — DO NOT use any external assertion lib)
    - .planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-RESEARCH.md (§"Required-vs-Optional Check Inventory" for the authoritative check ID list)
  </read_first>
  <action>
    Create `validators/scripts/test-init-checks.js`. Mirror the addTest framework from `test-cli.js` (no external libs). For each test, use `fs.mkdtempSync(path.join(os.tmpdir(), 'adp-init-checks-'))` to create an isolated tempdir; populate the minimum filesystem to exercise the check; call `runStrictChecks(tempdir)`; assert; `fs.rmSync(tempdir, { recursive: true, force: true })` in a finally block.

    Required test cases (one addTest each):
    1. `'init-checks: greenfield with all required artifacts returns ok=true'` — populate dirs, flow YAML, ledger, skill folders, constitution; expect `report.ok === true`, `report.failures.length === 0`.
    2. `'init-checks: missing flow YAML produces failure with id=flow.yaml.exists'` — omit the YAML; expect one failure with that id and `evidence.checkedPaths` including the expected path.
    3. `'init-checks: malformed flow YAML produces failure with id=flow.yaml.parse and evidence.parseError'` — write invalid YAML; assert `failures[i].evidence.parseError` is a non-empty string.
    4. `'init-checks: missing ledger produces failure with id=ledger.exists'`.
    5. `'init-checks: invalid ledger schema produces failure with id=ledger.schema and offendingLines'`.
    6. `'init-checks: missing prereq produces failure with category=tool and guidance populated from INSTRUCTIONS_DB'` — assert `result.guidance.purpose` and `result.guidance.verifyCommand` exist (Plan 01 enrichment).
    7. `'init-checks: localization scan flags @~/foo inside <execution_context>'` — write a SKILL.md with the bad pattern; assert `failures[i].id === 'localization.localPaths'` and `category === 'localization'`.
    8. `'init-checks: localization scan IGNORES literal ~ in documentation prose'` (Pitfall 1) — write a SKILL.md whose body text mentions `~` OUTSIDE an `<execution_context>` block; assert NO localization failure.
    9. `'init-checks: localization scan flags missing referenced file'` — write a SKILL.md with `@./missing/file.md` inside `<execution_context>`; assert failure with id=`localization.copiedRefs`.
    10. `'init-checks: instruction file missing subagent heading produces category=instruction (NOT tool)'` (D-15-15) — write CLAUDE.md without the heading; assert `category === 'instruction'`, NOT `'tool'`.
    11. `'init-checks: formatMarkdownGuide renders category=instruction failures with exact literal "Local workflow files incomplete"'` (F4 wording pin) — construct a report with a fake instruction failure; assert the rendered string contains the exact literal.
    12. `'init-checks: formatMarkdownGuide renders `~` literally, never resolves to absolute home'` — populate a tool failure; assert the rendered string contains `~` and does NOT contain `os.homedir()` output.
    13. `'init-checks: runStrictChecks survives EACCES on a skill folder'` (T-15-02-06) — `fs.chmodSync(skillDir, 0o000)` (POSIX only — skip on win32 AND skip when `process.getuid && process.getuid() === 0` per ENG review E3, since root ignores mode bits and the test would pass vacuously); call runStrictChecks; expect it returns without throwing and the failure has `evidence.parseError` containing the error code. Restore mode in finally.
    14. `'init-checks: runStrictChecks survives ENOENT mid-scan'` — populate a SKILL.md path that gets `fs.unlinkSync`'d between sandbox setup and the call (best-effort race simulation); expect no throw.
    15. `'init-checks: formatTerminal output is ≤120 chars per line'` — generate a report with 3 failures; split formatTerminal(report) by '\n'; assert every line.length <= 120.
    16. `'init-checks: constitution.exists passes when .ai/constitution.md present'` (ENG review E3) — write `.ai/constitution.md`; assert no failure with id=`constitution.exists`. Then delete it and re-run; assert a failure with that id appears.
    17. `'init-checks: featurePointer.active skipped when .specify/feature.json absent'` (ENG review E3) — first variant: no pointer file → no failure with id=`featurePointer.active`. Second variant: pointer file exists with `feature_directory: "specs/does-not-exist"` → failure with that id. Third variant: pointer file exists with valid `feature_directory` → no failure.

    All tests MUST end with explicit tempdir cleanup. Skip POSIX-only tests on `process.platform === 'win32'` (use `if (process.platform === 'win32') { return; }` early-return inside the test body — don't skip the addTest registration).

    Use CommonJS, 2-space indent, semicolons. Lazy-require `lib/init-checks.js` at top of file. No external test framework.
  </action>
  <verify>
    <automated>npm run test:init-checks</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "addTest('init-checks:" validators/scripts/test-init-checks.js` returns at least 14 (12 original + #16 + #17 per ENG review E3).
    - Source assertion: `grep -c "require('../../lib/init-checks')" validators/scripts/test-init-checks.js` returns exactly 1 (one shared require, not per-test).
    - Source assertion: tests do NOT call `spawnSync` or `child_process` — these are unit tests, not CLI tests.
    - Behavior assertion: `npm run test:init-checks` exits 0 with all tests passing.
    - Behavior assertion: total runtime under 5 seconds (tempdir-only, no child processes).
    - Test command: `npm test` exits 0 (full suite includes the new script).
  </acceptance_criteria>
  <done>
    `validators/scripts/test-init-checks.js` exercises every check ID with a pass and fail case, plus the FS-error rescue and Pitfall-1 regex. `npm run test:init-checks` runs in under 5 seconds and is wired into `npm test`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire test:init-checks into package.json</name>
  <files>package.json</files>
  <read_first>
    - package.json (locate the existing `scripts` block with `test`, `test:cli`, `test:validator`, `test:pipeline`)
  </read_first>
  <action>
    Add a new script entry: `"test:init-checks": "node validators/scripts/test-init-checks.js"`. Extend the existing `test` script to invoke `test:init-checks` after `test:cli` (or wherever the natural ordering puts it — after `test:validator` is fine). Do not introduce any new dependencies.
  </action>
  <verify>
    <automated>npm run test:init-checks && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "test:init-checks" package.json` returns at least 2 (script definition + invocation from `test`).
    - Behavior assertion: `npm test` runs `test:init-checks` as one of its steps and exits 0.
  </acceptance_criteria>
  <done>
    `npm run test:init-checks` works standalone and `npm test` includes it in the full suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tempdir under os.tmpdir() → host filesystem | Each test creates and cleans up an isolated tempdir. No write outside that tempdir. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Tampering | Test pollution via shared tempdir | mitigate | Each test uses `fs.mkdtempSync` for a unique tempdir; `fs.rmSync(..., {recursive:true, force:true})` in finally. No shared state. |
| T-15-05-02 | Availability | chmod 000 test leaves dir unreadable on failure | mitigate | The EACCES test wraps the `chmodSync(dir, 0o000)` + assertion in try/finally that restores mode to `0o700` before `rmSync`. Skip the test entirely on win32. |
| T-15-05-SC | Tampering | Supply chain | accept | No new packages added. |

</threat_model>

<verification>
- `npm run test:init-checks` exits 0.
- `npm test` exits 0 with the new script included.
- Total `test:init-checks` runtime under 5 seconds.
</verification>

<success_criteria>
- 12+ unit tests covering every check ID and the FS-error rescue.
- Tests run as direct require, no child process spawn.
- Pitfall 1 regex behavior locked in by tests #7 and #8.
- D-15-15 wording pin asserted by test #11.
- Wired into `npm test`.
</success_criteria>

<output>
Create `.planning/phases/15-strict-initialization-checks-and-detailed-installation-guide/15-05-SUMMARY.md` when done.
</output>
