---
phase: 16-context-budget-gate-and-subagent-orchestration-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/context-budget.js
  - lib/context-policy-validator.js
  - validators/scripts/test-context-budget.js
  - .ai/context-packs/.gitkeep
  - package.json
autonomous: true
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]
must_haves:
  truths:
    - "estimateBudget sums byte weights of declared artifacts, session logs, planning artifacts, context packs, and handoff files using fs.statSync"
    - "computeOutcome returns inline / context_pack_required / fresh_session_required based on configurable thresholds"
    - "stage_overrides in policy config bypass byte computation and force the configured outcome"
    - "validateContextPack rejects a pack missing any required field, a pack whose required_files reference a non-existent path, and a fan-out pack with overlapping write_targets and no coordination_note"
    - "D-16-07: the context-pack schema references canonical files by path — required_files and expected_outputs hold path strings (validated as path-shaped, not absolute, no '..'), and validateContextPack flags a pack that embeds a large inline file body instead of carrying a path reference"
    - "validateHandoffArtifact rejects a handoff whose context_pack_path does not exist or whose resume_stage / verification_commands are missing"
    - "validatePolicyConfig rejects inline_threshold_bytes >= pack_threshold_bytes and max_parallelism > 10"
    - "node validators/scripts/test-context-budget.js exits 0"
  artifacts:
    - path: "lib/context-budget.js"
      provides: "Deterministic budget estimation and policy-outcome decision"
      exports: ["estimateBudget", "computeOutcome", "loadPolicyConfig", "DEFAULT_POLICY"]
      min_lines: 80
    - path: "lib/context-policy-validator.js"
      provides: "Schema validation for policy config, context packs, fan-out, and handoff artifacts"
      exports: ["validatePolicyConfig", "validateContextPack", "validateHandoffArtifact"]
      min_lines: 100
    - path: "validators/scripts/test-context-budget.js"
      provides: "Unit coverage for budget estimation and policy validation"
      min_lines: 80
    - path: ".ai/context-packs/.gitkeep"
      provides: "Durable context-pack directory under .ai/ contract"
  key_links:
    - from: "lib/context-budget.js"
      to: "lib/flow-engine.js"
      via: "require checkArtifacts and resolveTemplatePath for path resolution before fs.statSync"
      pattern: "require\\(['\"]\\./flow-engine['\"]\\)"
    - from: "package.json"
      to: "validators/scripts/test-context-budget.js"
      via: "npm test runs all validators/scripts/test-*.js"
      pattern: "test-context-budget"
---

<objective>
Create the two pure foundation modules for the context budget and orchestration policy layer: `lib/context-budget.js` (deterministic file-size estimation + three-outcome policy decision) and `lib/context-policy-validator.js` (schema validation for policy config, context packs, fan-out groups, and fresh-session handoff artifacts). Ship a dedicated unit test that exercises every threshold boundary and every fail-closed validation rule, plus the durable `.ai/context-packs/` directory.

Purpose: Every downstream integration (flow-engine stage resolution, `adp doctor`/strict-init, instruction files) consumes these two modules. They must exist with stable exports and proven behavior before any wiring is done. This plan also satisfies the Wave 0 test/stub gaps named in RESEARCH.md so later plans extend rather than scaffold.

Output: `lib/context-budget.js`, `lib/context-policy-validator.js`, `validators/scripts/test-context-budget.js`, `.ai/context-packs/.gitkeep`, and a `package.json` test wiring confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-CONTEXT.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/context-budget.js (estimation + outcome + config loader)</name>
  <files>lib/context-budget.js</files>
  <read_first>
    - lib/tool-validator.js (module shape, DEFAULT/INSTRUCTIONS_DB constant pattern, structured-result-per-input pattern, silent-fallback philosophy)
    - lib/flow-engine.js (checkArtifacts and resolveTemplatePath signatures and exports; the try/catch fs.statSync pattern at lines 130-139; resolveTemplatePath at lines 97-104)
    - .ai/flows/rough-project-flow.yaml (flowStage shape: each stage has id and required_artifacts paths, some with template variables)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Budget Estimation Model table, threshold table, Policy Config Schema, Pattern 1)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (lib/context-budget.js section: imports, module shape, DEFAULT_POLICY constant, try/catch, template resolution)
  </read_first>
  <behavior>
    - Test: computeOutcome(0, 'execution', DEFAULT_POLICY) === 'inline'
    - Test: computeOutcome(50001, 'execution', DEFAULT_POLICY) === 'context_pack_required'
    - Test: computeOutcome(200001, 'execution', DEFAULT_POLICY) === 'fresh_session_required'
    - Test: stage_overrides { execution: { outcome: 'inline' } } forces 'inline' regardless of byte total
    - Test: estimateBudget over a temp stage with two real files returns totalBytes equal to the sum of the two file sizes and an inputs array with one entry per existing file
    - Test: estimateBudget treats a non-existent / unresolved-template path as 0 bytes and never throws
    - Test: loadPolicyConfig returns DEFAULT_POLICY when .ai/state/context-policy.json is absent; merges file values over defaults when present
  </behavior>
  <action>
    Implement the module in CommonJS with `'use strict';` and only `fs` and `path` requires (no child_process, no os). Export exactly `{ estimateBudget, computeOutcome, loadPolicyConfig, DEFAULT_POLICY }` per D-16-01 and D-16-02.

    Define `DEFAULT_POLICY` as a module-level constant with: `inline_threshold_bytes: 50000`, `pack_threshold_bytes: 200000`, `max_parallelism: 3`, `stage_overrides: {}`, and a `budget_inputs` object with all five boolean keys (`include_required_artifacts`, `include_session_logs`, `include_planning_artifacts`, `include_context_packs`, `include_handoff_files`) defaulting to `true`. These conservative defaults implement D-16-03 (bias toward packs before large stages; fresh-session when accumulated artifacts are large).

    `loadPolicyConfig(repoRoot)`: read `.ai/state/context-policy.json` relative to repoRoot; on absent file or parse error return a deep copy of DEFAULT_POLICY (silent fallback, never throw). When present, shallow-merge top-level keys over defaults and merge `budget_inputs` sub-keys over default `budget_inputs`.

    `estimateBudget(flowStage, repoRoot, variables)`: require `checkArtifacts` and `resolveTemplatePath` from `./flow-engine` (Don't Hand-Roll — do not reimplement path resolution or existence checks). When `flowStage` is falsy return `{ totalBytes: 0, inputs: [] }`. Otherwise: (1) use `checkArtifacts(flowStage, repoRoot, variables)` to resolve declared artifact paths, then for each existing result `fs.statSync(path.join(repoRoot, r.path)).size` inside try/catch (ENOENT → 0 bytes, never throw); (2) shallow-walk the input-category directories named in RESEARCH.md Budget Estimation Model table — `.ai/sessions/*.md`, `.planning/phases/<current-phase>/*.md`, `.ai/context-packs/*.json`, and `.ai/state/context-handoff.json` — gating each category on the corresponding `budget_inputs` flag from a loaded policy config. Use top-level (shallow) directory reads only; do NOT recurse (RESEARCH Sizing rule, avoids O(n) on large trees). Accumulate `{ path, bytes }` into `inputs` and sum into `totalBytes`. Always resolve template variables via `resolveTemplatePath` before any stat (Pitfall 2).

    `computeOutcome(totalBytes, stageId, policyConfig)`: if `policyConfig.stage_overrides[stageId]` exists and has an `outcome`, return that outcome unchanged (bypass computation — D-16-03 / RESEARCH Sizing rule). Otherwise apply thresholds: `<= inline_threshold_bytes` → `'inline'`; `<= pack_threshold_bytes` → `'context_pack_required'`; greater → `'fresh_session_required'`. Return one of the three literal strings only.

    Do NOT query any live token API, spawn subprocesses, or call the network (D-16-01, PROJECT.md offline constraint).
  </action>
  <verify>
    <automated>node -e "const m=require('./lib/context-budget'); const p=m.DEFAULT_POLICY; if(m.computeOutcome(0,'x',p)!=='inline'||m.computeOutcome(50001,'x',p)!=='context_pack_required'||m.computeOutcome(200001,'x',p)!=='fresh_session_required'){process.exit(1)} if(m.computeOutcome(999999,'x',{...p,stage_overrides:{x:{outcome:'inline'}}})!=='inline'){process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/context-budget.js` contains `module.exports = { estimateBudget, computeOutcome, loadPolicyConfig, DEFAULT_POLICY }`
    - The module requires `./flow-engine` and references `checkArtifacts` and `resolveTemplatePath` (does not reimplement them)
    - The module contains no `child_process`, `spawnSync`, `fetch`, or network require
    - `computeOutcome` returns `inline` at 0 bytes, `context_pack_required` at 50001, `fresh_session_required` at 200001 under DEFAULT_POLICY (verify command exits 0)
    - A `stage_overrides` entry forces its `outcome` regardless of byte total (verify command exits 0)
    - `estimateBudget` wraps every `fs.statSync` in try/catch and never throws on a missing path
  </acceptance_criteria>
  <done>estimateBudget, computeOutcome, loadPolicyConfig, and DEFAULT_POLICY are exported; threshold boundaries and stage-override bypass behave per the verify command; no subprocess or network use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lib/context-policy-validator.js (fail-closed schema validation)</name>
  <files>lib/context-policy-validator.js</files>
  <read_first>
    - lib/flow-engine.js (validateLedger at lines 16-51: the { valid, errors } return shape and field-check-push pattern)
    - lib/init-checks.js (lines 127-136: JSON parse with error capture wrapping)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Context-Pack JSON Schema, Policy Config Schema validation rules, Fresh-Session Handoff schema, D-16-19 unsafe conditions, Pitfall 3 and Pitfall 4, Security Domain path-traversal threat)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (lib/context-policy-validator.js section: validation return shape, JSON parse with error capture, module exports)
  </read_first>
  <behavior>
    - Test: validatePolicyConfig passes a config with inline_threshold_bytes 50000 < pack_threshold_bytes 200000 and max_parallelism 3
    - Test: validatePolicyConfig fails when inline_threshold_bytes >= pack_threshold_bytes
    - Test: validatePolicyConfig fails when max_parallelism > 10 or not a positive integer
    - Test: validatePolicyConfig fails when a stage_overrides value has an outcome not in { inline, context_pack_required, fresh_session_required }
    - Test: validateContextPack passes a pack with all required fields and an empty omissions array
    - Test: validateContextPack fails on a missing required field (e.g. no validation_commands)
    - Test: validateContextPack fails when a required_files path does not exist on disk
    - Test: validateContextPack fails when a required_files or expected_outputs path is absolute or contains '..'
    - Test (D-16-07 reference-by-path): validateContextPack treats required_files and expected_outputs entries as path strings; a pack whose required_files entry is not a string path (e.g. an object/array carrying an embedded inline file body, or a string exceeding a path-length sanity bound such that it is a pasted file body rather than a path) is flagged as invalid
    - Test: validateContextPack with siblingPacks fails when two packs in the same fanout group share a write_target and coordination_note is empty
    - Test: validateContextPack fails when subagent_fanout is present but sequential_inline_fallback or join_owner is missing
    - Test: validateHandoffArtifact passes a valid handoff and fails when context_pack_path does not exist, resume_stage is missing, or verification_commands is empty
  </behavior>
  <action>
    Implement in CommonJS with `'use strict';` and only `fs` and `path` requires. Export exactly `{ validatePolicyConfig, validateContextPack, validateHandoffArtifact }`. Every function returns `{ valid: boolean, errors: string[] }` mirroring `validateLedger`. Wrap every `fs.readFileSync` + `JSON.parse` in try/catch; a parse failure returns `{ valid: false, errors: [e.message] }` (fail closed — D-16-19, RESEARCH Security Domain "report as check failure, not process crash"). All three functions accept either a file path (read+parse) or, where convenient, the already-parsed object — choose file-path-in signatures matching RESEARCH: `validatePolicyConfig(fullPath)`, `validateContextPack(fullPath, siblingPacks)`, `validateHandoffArtifact(fullPath, knownStageIds)`.

    `validatePolicyConfig`: enforce D-16-17 rules from RESEARCH Policy Config Schema — `inline_threshold_bytes` positive integer and strictly less than `pack_threshold_bytes`; `pack_threshold_bytes` positive integer; `max_parallelism` positive integer `<= 10` (clamp sanity cap, Security Domain V5); every `stage_overrides[*].outcome` must be one of the three known outcome strings. Push a distinct error string per violation.

    `validateContextPack`: required fields per RESEARCH (D-16-05) — `schema_version`, `stage_id`, `objective`, `required_files`, `expected_outputs`, `validation_commands`, `stop_conditions`, and `omissions` (must be present; an empty array `[]` is valid per Pitfall 3 / D-16-08). Enforce D-16-07 reference-by-path shape: `required_files` and `expected_outputs` must be arrays of path STRINGS, not embedded file bodies — reject an entry that is not a string, and reject a string that is not path-shaped (apply a path-length sanity bound — e.g. an entry longer than a reasonable path max such as 1024 chars is treated as a pasted file body, not a path reference — and push a distinct "embeds file body instead of path reference (D-16-07)" error). For every string entry in `required_files` and `expected_outputs`, reject paths that are absolute (leading `/`) or contain `..` (path traversal — Security Domain Tampering). For `required_files` entries, the referenced path must exist on disk relative to the repo root derived from `fullPath` (existence check — CTX-02). If `subagent_fanout` is present (CTX-03 / D-16-19): require `sequential_inline_fallback` field present, require `join_owner` present, and require non-empty `write_targets`; when `siblingPacks` is provided, compare this pack's `write_targets` against every sibling in the same `subagent_fanout.group_id` — if any write_target overlaps AND this pack's `coordination_note` is absent/empty, push a fail error (Pitfall 4 cross-file check, D-16-19 unsafe condition 1).

    `validateHandoffArtifact`: required fields `schema_version`, `resume_stage`, `next_skill`, `context_pack_path`, `verification_commands` (D-16-14). Fail closed (D-16-19): `verification_commands` must be a non-empty array; `context_pack_path` must exist on disk; when `knownStageIds` is provided, `resume_stage` must be one of them (D-16-15 preserves a real stage). Reject absolute/`..` `context_pack_path`.

    No LLM evaluation, no subprocess, no network (PROJECT.md, D-16-06 cross-ref). All decisions are deterministic field and filesystem checks.
  </action>
  <verify>
    <automated>node -e "const v=require('./lib/context-policy-validator'); const fs=require('fs'),os=require('os'),path=require('path'); const d=fs.mkdtempSync(path.join(os.tmpdir(),'cpv-')); fs.writeFileSync(path.join(d,'bad.json'),JSON.stringify({schema_version:'1.0',inline_threshold_bytes:300000,pack_threshold_bytes:200000,max_parallelism:99})); const r=v.validatePolicyConfig(path.join(d,'bad.json')); if(r.valid){console.error('should be invalid');process.exit(1)} if(r.errors.length<2){console.error('expected multiple errors');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/context-policy-validator.js` contains `module.exports = { validatePolicyConfig, validateContextPack, validateHandoffArtifact }`
    - All three functions return an object with boolean `valid` and string-array `errors`
    - Every `JSON.parse` is inside a try/catch; malformed JSON yields `{ valid: false, errors: [...] }` and never throws
    - `validatePolicyConfig` flags both `inline_threshold_bytes >= pack_threshold_bytes` and `max_parallelism > 10` (verify command exits 0)
    - `validateContextPack` rejects absolute paths and paths containing `..` in `required_files`/`expected_outputs`
    - D-16-07 reference-by-path: `required_files` and `expected_outputs` are validated as arrays of path strings — a non-string entry, or a string longer than the path-length sanity bound (an embedded file body rather than a path reference), produces a distinct error; a test in test-context-budget.js asserts such a pack is flagged invalid
    - `validateContextPack` accepts an `omissions: []` empty array as valid but fails when `omissions` is absent
    - Fan-out cross-file overlap with empty `coordination_note` produces an error when `siblingPacks` is supplied
  </acceptance_criteria>
  <done>The three validators return { valid, errors }, fail closed on malformed JSON, enforce all D-16-17 and D-16-19 rules, enforce D-16-07 reference-by-path shape for required_files/expected_outputs, and reject path-traversal paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add test-context-budget.js, .ai/context-packs/.gitkeep, and confirm npm test wiring</name>
  <files>validators/scripts/test-context-budget.js, .ai/context-packs/.gitkeep, package.json</files>
  <read_first>
    - validators/scripts/test-flow-engine.js (assert / assertDeepEqual runner boilerplate, temp-dir fixture pattern, section-header comment style, summary+exit pattern)
    - package.json (the test script — confirm it globs validators/scripts/test-*.js so the new file is picked up; if it lists files explicitly, add test-context-budget.js)
    - lib/context-budget.js (the exports just implemented in Task 1)
    - lib/context-policy-validator.js (the exports just implemented in Task 2)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Phase Requirements -> Test Map; Test Pattern code example)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (test-context-budget.js section: header/imports, runner boilerplate, temp-dir prefix context-budget-test-, summary label)
  </read_first>
  <action>
    Create `validators/scripts/test-context-budget.js` mirroring `test-flow-engine.js` exactly: `'use strict';`, require `fs`/`path`/`os`, require the four exports from `../../lib/context-budget` and the three from `../../lib/context-policy-validator`, the `assert` and `assertDeepEqual` helpers with `passed`/`failed` counters, `// ===` section headers with `console.log('--- name ---')`, a temp-dir fixture using prefix `context-budget-test-` (mkdtempSync + writeFileSync + cleanup unlink/rmdir), and a final `console.log(\`Context budget tests: ${passed} passed, ${failed} failed\`)` followed by `if (failed > 0) { process.exit(1); }`.

    Cover every row in RESEARCH Phase Requirements -> Test Map that maps to this file (CTX-01 estimateBudget/computeOutcome/stage_overrides; CTX-02 validateContextPack valid/missing-field/missing-ref; CTX-03 fan-out overlap and missing sequential_inline_fallback; CTX-04 validateHandoffArtifact valid and missing context_pack_path; plus validatePolicyConfig threshold and max_parallelism rules). Include an explicit D-16-07 reference-by-path case: assert that a context pack whose `required_files` entry embeds a large inline file body (a non-string entry, or a string exceeding the path-length sanity bound) is flagged invalid by `validateContextPack`, and that a pack carrying ordinary relative path strings in `required_files`/`expected_outputs` validates on that dimension. For tests that need real files (estimateBudget byte sums, required_files existence, context_pack_path existence) create them in the temp dir and clean up after.

    Create `.ai/context-packs/.gitkeep` (empty file) so the durable context-pack directory exists under the `.ai/` contract (D-16-06, RESEARCH Wave 0 Gaps; A4 — the planner adds this path).

    Confirm `package.json`'s `test` script will execute the new file. If it uses a glob over `validators/scripts/test-*.js`, no change is needed — note that in the SUMMARY. If it enumerates files explicitly, add `test-context-budget.js` to the list.
  </action>
  <verify>
    <automated>node validators/scripts/test-context-budget.js && test -f .ai/context-packs/.gitkeep && echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `node validators/scripts/test-context-budget.js` exits 0 and prints `Context budget tests: N passed, 0 failed`
    - The test file contains sections covering computeOutcome thresholds, stage_overrides bypass, estimateBudget byte sums, validateContextPack required-field and missing-ref failures, the D-16-07 reference-by-path case (embedded file body flagged invalid; path-string entries accepted), fan-out overlap failure, and validateHandoffArtifact missing-context_pack_path failure
    - `.ai/context-packs/.gitkeep` exists
    - `npm test` includes the new test file (glob match confirmed, or file added to an explicit list)
  </acceptance_criteria>
  <done>The dedicated unit suite passes, the context-packs directory exists, and npm test picks up the new file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → validator | JSON/YAML config and context-pack files read from `.ai/` are untrusted structured input parsed by the new modules |
| context-pack path fields → filesystem | `required_files` / `expected_outputs` / `context_pack_path` strings are used to derive filesystem reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial | lib/context-policy-validator.js JSON.parse / fs.readFileSync | mitigate | Wrap every parse/read in try/catch; malformed input returns `{ valid: false, errors: [msg] }` — never crashes the process (RESEARCH Security Domain) |
| T-16-02 | Tampering | context-pack `required_files`/`expected_outputs` paths | mitigate | validateContextPack rejects absolute paths (leading `/`) and any path containing `..` so reads cannot escape repo root; also enforces D-16-07 path-string shape so embedded file bodies are rejected rather than treated as paths |
| T-16-03 | Tampering | fresh-session handoff `context_pack_path` | mitigate | validateHandoffArtifact rejects absolute/`..` paths and requires the referenced pack to exist (D-16-19 resume target present) |
| T-16-04 | Information Disclosure | estimateBudget directory walk | accept | Shallow top-level reads only of known `.ai/` and `.planning/` paths; no recursion, no content read (only file sizes) — low value, local-only tool |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced this phase (RESEARCH Package Legitimacy Audit N/A); no install tasks — nothing to verify |
</threat_model>

<verification>
- `node validators/scripts/test-context-budget.js` exits 0.
- `node -e` smoke checks for computeOutcome thresholds and validatePolicyConfig fail-closed (per task verify commands) exit 0.
- `npm test` runs without regressions and includes the new test file.
- Neither new module references `child_process`, `spawnSync`, `fetch`, or any network/LLM API.
</verification>

<success_criteria>
- `lib/context-budget.js` exports estimateBudget, computeOutcome, loadPolicyConfig, DEFAULT_POLICY and reuses checkArtifacts/resolveTemplatePath from flow-engine.
- `lib/context-policy-validator.js` exports the three validators, all returning `{ valid, errors }`, all fail-closed, path-traversal-safe, and enforcing D-16-07 reference-by-path shape.
- The dedicated unit suite passes and is picked up by npm test.
- `.ai/context-packs/` exists as a durable directory.
- No subprocess, network, or LLM-as-judge logic introduced (PROJECT.md, D-16-01).
</success_criteria>

<output>
Create `.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-01-SUMMARY.md` when done.
</output>
