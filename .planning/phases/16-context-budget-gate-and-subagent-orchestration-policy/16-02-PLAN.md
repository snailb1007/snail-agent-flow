---
phase: 16-context-budget-gate-and-subagent-orchestration-policy
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/flow-engine.js
  - validators/scripts/test-flow-engine.js
autonomous: true
requirements: [CTX-01]
must_haves:
  truths:
    - "resolveNextStage returns a contextPolicy object alongside ledgerStage and flowStage when a valid stage resolves"
    - "resolveNextStage still works when called with only (ledger, flowDefinition) — repoRoot and variables are optional"
    - "formatStageInstruction appends a CONTEXT POLICY block with the outcome and estimated size when a contextPolicy argument is supplied"
    - "context_pack_required and fresh_session_required outcomes print their required action lines; inline prints no action line"
    - "node validators/scripts/test-flow-engine.js exits 0"
  artifacts:
    - path: "lib/flow-engine.js"
      provides: "Stage resolution that emits the context policy decision inline with the next stage instruction"
      contains: "contextPolicy"
    - path: "validators/scripts/test-flow-engine.js"
      provides: "Extended coverage proving resolveNextStage returns contextPolicy and formatStageInstruction renders the block"
  key_links:
    - from: "lib/flow-engine.js"
      to: "lib/context-budget.js"
      via: "require estimateBudget, computeOutcome, loadPolicyConfig inside resolveNextStage"
      pattern: "require\\(['\"]\\./context-budget['\"]\\)"
    - from: "resolveNextStage"
      to: "formatStageInstruction"
      via: "contextPolicy is passed through as the third argument"
      pattern: "formatStageInstruction\\("
---

<objective>
Wire the budget gate into flow-stage resolution so the policy decision is emitted alongside the next stage instruction (D-16-04: integrated, not an advisory side-note). Extend `resolveNextStage` to compute and return a `contextPolicy` object, and extend `formatStageInstruction` to render a CONTEXT POLICY block with the outcome, estimated size, and the required agent action.

Purpose: D-16-04 requires the budget decision to be reported when the next stage is resolved, before the agent starts work. This is the consumer that turns the pure module from Plan 01 into observable flow-engine behavior. The change must be strictly additive so existing callers in `bin/adp.js` and the existing test suite keep working (RESEARCH Pitfall 1 and Assumption A2).

Output: modified `lib/flow-engine.js` and extended `validators/scripts/test-flow-engine.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-CONTEXT.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend resolveNextStage and formatStageInstruction in lib/flow-engine.js</name>
  <files>lib/flow-engine.js</files>
  <read_first>
    - lib/flow-engine.js (resolveNextStage at lines 61-88; formatStageInstruction at line 250; module.exports at line 343; the early `return null` path; the footer line that closes the stage block)
    - lib/context-budget.js (estimateBudget, computeOutcome, loadPolicyConfig signatures and DEFAULT_POLICY — built in Plan 01)
    - bin/adp.js (existing callers of resolveNextStage and formatStageInstruction — confirm they destructure { ledgerStage, flowStage } and will tolerate an additional contextPolicy key; the handleStatus path near line 579)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Pattern 2 resolveNextStage extension, Pattern 3 formatStageInstruction extension, Pitfall 1 backward compatibility, Assumption A2)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (resolveNextStage extension section: optional trailing params with defaults; formatStageInstruction extension section: append after the footer line)
  </read_first>
  <behavior>
    - Test: resolveNextStage(ledger, flowDef) called with only two args still returns a truthy object with ledgerStage and flowStage (no throw)
    - Test: resolveNextStage on a ledger with a resolvable stage returns a contextPolicy object containing an outcome that is one of inline / context_pack_required / fresh_session_required and a numeric estimatedBytes
    - Test: resolveNextStage returns null (unchanged) when ledger has no stages array
    - Test: formatStageInstruction(flowStage, ledgerStage) with no contextPolicy renders the existing block and no CONTEXT POLICY section
    - Test: formatStageInstruction(flowStage, ledgerStage, { outcome: 'context_pack_required', estimatedBytes: 87432 }) includes the string 'CONTEXT POLICY', the outcome, and the .ai/context-packs action line
    - Test: formatStageInstruction with outcome fresh_session_required includes the .ai/state/context-handoff.json STOP action line
  </behavior>
  <action>
    Extend `resolveNextStage(ledger, flowDefinition)` to `resolveNextStage(ledger, flowDefinition, repoRoot, variables)`. Make `repoRoot` and `variables` OPTIONAL with defaults (`repoRoot = repoRoot || process.cwd()`, `variables = variables || {}`) so the existing two-argument callers in `bin/adp.js` and `test-flow-engine.js` keep working (Pitfall 1, A2). Leave the existing `return null` early-exit path and all existing priority/stage-selection logic unchanged. After a valid `ledgerStage` + `flowStage` are resolved, require `./context-budget`, call `loadPolicyConfig(repoRoot)`, `estimateBudget(flowStage, repoRoot, variables)` (destructure `{ totalBytes, inputs }`), and `computeOutcome(totalBytes, flowStage ? flowStage.id : null, policyConfig)`. Return `{ ledgerStage, flowStage, contextPolicy: { outcome, estimatedBytes: totalBytes, inputs, policyConfig } }`. Attach `contextPolicy` only to the valid result — never to the `null` path (D-16-04).

    Extend `formatStageInstruction(flowStage, ledgerStage)` to accept an optional third param `contextPolicy`. Keep all existing lines unchanged. After the existing footer line that closes the stage block, when `contextPolicy` is truthy, append: a blank line, a `─── CONTEXT POLICY ───` header, `Outcome:   <outcome>`, `Est. size: <KB>` (estimatedBytes/1024 to 1 decimal). When outcome is `context_pack_required`, append an action line instructing creation of `.ai/context-packs/<stage>-<timestamp>.json` before starting work, referencing files by path and recording omissions (D-16-05, D-16-08). When outcome is `fresh_session_required`, append an action line instructing the agent to write `.ai/state/context-handoff.json` then STOP this session, noting a new session resumes from this stage (D-16-14, D-16-15). Close with a `──────────────────────` footer. For `inline`, append no action line.

    Do NOT add new entries to `module.exports` — `contextPolicy` rides inside the `resolveNextStage` return value (RESEARCH Integration Points: "Add nothing"). Do NOT introduce any subprocess, network, or token-API call.
  </action>
  <verify>
    <automated>node -e "const fe=require('./lib/flow-engine'); const out=fe.formatStageInstruction({id:'execution',name:'Execution'},{id:'execution',status:'in_progress'},{outcome:'fresh_session_required',estimatedBytes:250000}); if(!out.includes('CONTEXT POLICY')||!out.includes('context-handoff.json')){process.exit(1)} const plain=fe.formatStageInstruction({id:'x',name:'X'},{id:'x',status:'pending'}); if(plain.includes('CONTEXT POLICY')){process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/flow-engine.js` `resolveNextStage` signature includes optional `repoRoot` and `variables` params with `|| process.cwd()` / `|| {}` defaults
    - `resolveNextStage` requires `./context-budget` and returns a `contextPolicy` key with `outcome`, `estimatedBytes`, `inputs`, `policyConfig` on the valid path
    - The `return null` path for a stage-less ledger is unchanged (no contextPolicy attached)
    - `formatStageInstruction` accepts a third `contextPolicy` param and appends a `CONTEXT POLICY` block only when it is truthy (verify command exits 0)
    - `fresh_session_required` output contains `context-handoff.json`; `context_pack_required` output contains `.ai/context-packs/`; `inline` and absent-policy outputs contain neither
    - No new symbols added to `module.exports`
  </acceptance_criteria>
  <done>resolveNextStage emits contextPolicy additively, formatStageInstruction renders the policy block per outcome, and two-argument callers are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test-flow-engine.js to cover contextPolicy emission and rendering</name>
  <files>validators/scripts/test-flow-engine.js</files>
  <read_first>
    - validators/scripts/test-flow-engine.js (existing resolveNextStage tests, createMockLedger / createMockFlowDefinition helpers, assert/assertDeepEqual, temp-dir fixture, summary+exit)
    - lib/flow-engine.js (the resolveNextStage and formatStageInstruction changes from Task 1)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Phase Requirements -> Test Map row: "resolveNextStage returns contextPolicy in result" — test-flow-engine.js extend)
  </read_first>
  <action>
    Add a new `// ===` test section (e.g. `--- resolveNextStage contextPolicy ---`) to the existing `test-flow-engine.js` using the file's existing helpers and counters. Assert: (1) `resolveNextStage` called with only `(ledger, flowDef)` still returns a truthy result with `ledgerStage` and `flowStage` (backward compatibility, Pitfall 1); (2) the returned result has a `contextPolicy` object whose `outcome` is one of the three known strings and whose `estimatedBytes` is a number; (3) `resolveNextStage` returns `null` for a ledger with no stages array (unchanged behavior). Add a `--- formatStageInstruction contextPolicy ---` section asserting the rendered string includes `CONTEXT POLICY` and the correct action line for `context_pack_required` and for `fresh_session_required`, and excludes the block when no contextPolicy is passed. Reuse the existing temp-dir fixture if a real repoRoot is needed; otherwise rely on the default `process.cwd()` (which returns a real but harmless estimate). Do not alter the existing summary/exit lines — the new asserts feed the same `passed`/`failed` counters.
  </action>
  <verify>
    <automated>node validators/scripts/test-flow-engine.js && echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `validators/scripts/test-flow-engine.js` contains a section asserting `resolveNextStage` returns a `contextPolicy` with a valid `outcome` and numeric `estimatedBytes`
    - It asserts the two-argument call still works and the no-stages ledger still returns `null`
    - It asserts `formatStageInstruction` renders/omits the CONTEXT POLICY block per the contextPolicy argument
    - `node validators/scripts/test-flow-engine.js` exits 0 with no failures
  </acceptance_criteria>
  <done>The existing flow-engine test suite covers the additive contextPolicy behavior and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger/flow definition → resolveNextStage | Ledger JSON and flow-definition stage data drive which paths estimateBudget stats; both are local repo files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial | resolveNextStage budget call on malformed stage | mitigate | estimateBudget (Plan 01) wraps stats in try/catch and returns 0 bytes; resolveNextStage attaches contextPolicy only on the valid path, so a null/empty stage never reaches the budget call |
| T-16-06 | Tampering | additive signature change breaking existing callers | mitigate | repoRoot/variables are optional with defaults; module.exports unchanged; existing two-arg callers and tests verified to still pass |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependencies (RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `node validators/scripts/test-flow-engine.js` exits 0.
- The `node -e` smoke check for formatStageInstruction rendering exits 0.
- `npm test` passes with no regressions in the existing CLI or flow-engine suites.
- `git grep -n "module.exports" lib/flow-engine.js` shows no newly added export symbols.
</verification>

<success_criteria>
- resolveNextStage returns contextPolicy additively while remaining backward compatible with two-argument callers (D-16-04).
- formatStageInstruction renders the CONTEXT POLICY block with the correct action per outcome.
- The flow-engine test suite covers the new behavior and passes.
- No subprocess, network, or token-API logic introduced.
</success_criteria>

<output>
Create `.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-02-SUMMARY.md` when done.
</output>
