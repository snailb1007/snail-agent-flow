---
phase: 16-context-budget-gate-and-subagent-orchestration-policy
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - lib/init-checks.js
  - bin/adp.js
  - validators/scripts/test-init-checks.js
  - validators/scripts/test-cli.js
autonomous: true
requirements: [CTX-02, CTX-03, CTX-04, CTX-05]
must_haves:
  truths:
    - "runStrictChecks emits policy.config.exists (warn-only), policy.config.schema, context.packs.schema, context.packs.refs, context.packs.fanout.conflicts, handoff.exists, handoff.schema, and instructions.contextPolicySection check results using the existing structured-check schema"
    - "adp doctor exits non-zero when a discovered context pack is missing a required field or references a non-existent required_files path"
    - "adp doctor exits non-zero when two context packs in the same fan-out group have overlapping write_targets and no coordination_note"
    - "adp init creates .ai/state/context-policy.json with conservative defaults only when absent (idempotent), never overwriting an existing one"
    - "adp init appends a '## Context Budget and Subagent Orchestration Policy' section to CLAUDE.md, GEMINI.md, AGENTS.md only when absent, without duplicating"
    - "An absent policy config is a warning, not a failure (defaults apply)"
    - "The fresh-session handoff artifact is written to the fixed well-known state path .ai/state/context-handoff.json, which satisfies the D-16-16 state-metadata linkage clause by convention (handoff.exists/handoff.schema validate exactly that fixed path)"
    - "node validators/scripts/test-init-checks.js and node validators/scripts/test-cli.js exit 0"
  artifacts:
    - path: "lib/init-checks.js"
      provides: "Policy config, context pack, fan-out, and handoff validation checks wired into runStrictChecks"
      contains: "context-policy-validator"
    - path: "bin/adp.js"
      provides: "Idempotent context-policy.json creation and context-policy instruction-file section on init"
      contains: "context-policy.json"
  key_links:
    - from: "lib/init-checks.js"
      to: "lib/context-policy-validator.js"
      via: "require validatePolicyConfig, validateContextPack, validateHandoffArtifact in runStrictChecks"
      pattern: "require\\(['\"]\\./context-policy-validator['\"]\\)"
    - from: "bin/adp.js handleInit"
      to: ".ai/state/context-policy.json"
      via: "writeFileSync only when the file is absent"
      pattern: "context-policy\\.json"
    - from: "bin/adp.js handleDoctor"
      to: "runStrictChecks"
      via: "runAndReport surfaces new policy/pack/handoff check failures"
      pattern: "runAndReport"
---

<objective>
Close the validation and onboarding loop: wire the three validators from Plan 01 into `runStrictChecks` as new structured check IDs (D-16-17, D-16-18, D-16-19), make `adp init` create the conservative-default policy config idempotently and append the context-policy instruction-file section (D-16-03, D-16-06, CTX-03 instruction shape), and extend the init-checks and CLI test suites to prove fail-closed behavior end to end.

Purpose: D-16-18 requires `adp doctor` and strict init to detect missing/malformed context-policy artifacts; D-16-19 requires validation to fail closed for unsafe orchestration (overlapping write targets, missing packs, handoff without a resume target). This plan makes the policy layer observable and enforced through the existing CLI surfaces without new commands (RESEARCH Integration Points: doctor/init pick up new check IDs automatically via runAndReport).

D-16-16 state-metadata clause: the handoff path has two linkage halves — (1) reported in terminal/chat output, covered by 16-02 `formatStageInstruction`; (2) "where applicable, linked from ledger or state metadata." Per RESEARCH Open Question 2 (RESOLVED), this second half is satisfied **by convention** in this plan: the fresh-session handoff is always written to the fixed well-known state path `.ai/state/context-handoff.json`. That fixed, well-known path IS the state-metadata linkage — a new session resumes deterministically by reading the well-known path, with no per-stage lookup required. This plan's `handoff.exists`/`handoff.schema` checks validate exactly that fixed path (D-16-15 resume target). Optional ledger annotation of `context_policy.handoff_path` for `fresh_session_required` is reinforcement only and is out of scope here.

Output: modified `lib/init-checks.js`, `bin/adp.js`, and extended `validators/scripts/test-init-checks.js` and `validators/scripts/test-cli.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-CONTEXT.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md
@.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire policy/pack/handoff checks into runStrictChecks (lib/init-checks.js)</name>
  <files>lib/init-checks.js</files>
  <read_first>
    - lib/init-checks.js (runStrictChecks at line 13; the structured check-push pattern at lines 62-74; the two-phase exists->schema pattern at lines 123-150; the instructions.subagentSection heading-presence check at lines 366-395; the failures/warnings split at lines 452-461; module.exports at line 637)
    - lib/context-policy-validator.js (validatePolicyConfig, validateContextPack, validateHandoffArtifact signatures from Plan 01 — file-path-in, { valid, errors } out)
    - lib/flow-engine.js (resolveTemplatePath and any helper to read flow definition stage IDs for handoff.resume_stage validation)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Pattern 4 runStrictChecks integration; the new-check-ID table; runStrictChecks Check Object Pattern example; Pitfall 4 cross-file fan-out grouping)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (lib/init-checks.js section: check-push pattern, two-phase check, instructions.contextPolicySection heading-presence pattern)
  </read_first>
  <action>
    Inside `runStrictChecks(repoRoot, opts)`, before the `return`, require `./context-policy-validator` and push the new check results using the EXISTING structured schema `{ id, category: 'artifact', required, passed, subject, evidence, guidance: null }`. Add these check IDs per RESEARCH new-check table:

    - `policy.config.exists` — subject `.ai/state/context-policy.json`, `required: false`, `passed: true` always (absence is a warning conveyed via `evidence.parseError: 'Policy config absent — using defaults.'`). Implements D-16-18 warn-only-on-absence (defaults are safe).
    - `policy.config.schema` — only when the config file exists; `required: true`; `passed: validatePolicyConfig(fullPath).valid`; errors joined into `evidence.parseError`.
    - Context packs: discover all `.ai/context-packs/*.json` (shallow readdir, skip `.gitkeep`). Group by `subagent_fanout.group_id` (Pitfall 4 — overlap is only visible across siblings). For each pack push `context.packs.schema` (`required: true`, `validateContextPack(fullPath)` for required fields) and `context.packs.refs` (`required: true`, fails when a `required_files` path does not exist — surfaced through validateContextPack). Push `context.packs.fanout.conflicts` (`required: true`) by calling `validateContextPack(fullPath, siblingPacks)` with the sibling list from the same group so cross-file `write_targets` overlap with empty `coordination_note` fails (D-16-19 unsafe condition 1).
    - `handoff.exists` — subject `.ai/state/context-handoff.json`, `required: false` (warn-only; only meaningful when ledger marks a pending handoff). The subject MUST be exactly this fixed well-known path: per D-16-16 (RESEARCH Open Question 2, RESOLVED), the fixed `.ai/state/context-handoff.json` path is the state-metadata linkage by convention, so validation always targets it rather than a path resolved from ledger metadata.
    - `handoff.schema` — only when the handoff file exists; `required: true`; `passed: validateHandoffArtifact(fullPath, knownStageIds).valid` where `knownStageIds` is the list of stage IDs from the flow definition so `resume_stage` is validated (D-16-15, D-16-16, D-16-19 resume target).

    Add an `instructions.contextPolicySection` check mirroring the existing `instructions.subagentSection` block (lines 366-395): for each of `CLAUDE.md`, `GEMINI.md`, `AGENTS.md` that exists, `required: true`, `passed: content.includes('## Context Budget and Subagent Orchestration Policy')`, with a `parseError` naming the missing heading when absent.

    Wrap every `fs.readFileSync`/`JSON.parse`/`fs.statSync` in try/catch and surface failures as `passed: false` check results, never as thrown exceptions (T-16-01 fail-closed, RESEARCH Security Domain). Reuse the failures/warnings split already at the end of runStrictChecks — `required: true` failures land in `report.failures` (blocks doctor/init), `required: false` failures land in `report.warnings`. Do not change `module.exports`; callers consume the full `results` array.
  </action>
  <verify>
    <automated>node -e "const {runStrictChecks}=require('./lib/init-checks'); const r=runStrictChecks(process.cwd()); const ids=r.results.map(x=>x.id); for(const need of ['policy.config.exists','instructions.contextPolicySection']){ if(!ids.includes(need)){console.error('missing '+need);process.exit(1)} } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/init-checks.js` requires `./context-policy-validator` inside `runStrictChecks`
    - `runStrictChecks` results include the IDs `policy.config.exists`, `policy.config.schema` (when config present), `context.packs.schema`, `context.packs.refs`, `context.packs.fanout.conflicts`, `handoff.exists`, `handoff.schema` (when handoff present), and `instructions.contextPolicySection`
    - `policy.config.exists` is `required: false` and an absent config does not produce a failure (lands in warnings)
    - The `handoff.exists` and `handoff.schema` checks target the fixed well-known path `.ai/state/context-handoff.json` (the D-16-16 state-metadata linkage by convention per RESEARCH Open Question 2 RESOLVED), not a path resolved from ledger metadata
    - Context-pack validation groups packs by `subagent_fanout.group_id` before the cross-file fan-out conflict check
    - Every filesystem/JSON operation is inside try/catch; malformed artifacts produce `passed: false`, not a crash
    - `module.exports` of `lib/init-checks.js` is unchanged
  </acceptance_criteria>
  <done>runStrictChecks emits all new policy/pack/handoff/instruction check IDs using the existing schema, fails closed on malformed artifacts, validates the fixed `.ai/state/context-handoff.json` path (D-16-16 state-metadata linkage), and treats an absent config as a warning.</done>
</task>

<task type="auto">
  <name>Task 2: adp init creates context-policy.json idempotently and appends the context-policy instruction section</name>
  <files>bin/adp.js</files>
  <read_first>
    - bin/adp.js (handleInit at line 68; the .ai/state mkdir at line 73; the flow-ledger.json idempotent create at lines 176-197 — mirror this "create only if absent" pattern; appendSubagentGuidelines at lines 880-912 — mirror this heading-presence-guarded append; the appendSubagentGuidelines call site at line 231; handleDoctor at line 633; runAndReport at line 678)
    - lib/context-budget.js (DEFAULT_POLICY shape — the context-policy.json contents should match these defaults)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Policy Config Schema recommended contents; Instruction Files section — the 6 bullet rules for the new section; Pitfall 5 duplicate-append guard; Open Question 1 recommendation to merge/supersede the Phase 14 heading)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-PATTERNS.md (.ai/state/context-policy.json config artifact section: JSON style, written only if absent; Idempotent Section Append in Instruction Files)
  </read_first>
  <action>
    In `handleInit`, after the existing flow-ledger creation block, add an idempotent context-policy creation mirroring the ledger pattern (lines 176-197): compute `path.join(repoRoot, '.ai/state/context-policy.json')`; if it does NOT exist, `fs.writeFileSync` it with `JSON.stringify(config, null, 2) + '\n'` where `config` matches the RESEARCH Policy Config Schema (`schema_version: '1.0'`, `inline_threshold_bytes: 50000`, `pack_threshold_bytes: 200000`, `max_parallelism: 3`, empty `stage_overrides`, the five `budget_inputs` booleans all true, and the conservative-defaults `note`). If it already exists, log a skip message and do NOT overwrite (D-16-06 durable, no competing source of truth; PATTERNS idempotent rule). `.ai/state` already exists via line 73.

    Add an `appendContextPolicyGuidelines(repoRoot)` function mirroring `appendSubagentGuidelines` (lines 880-912) and call it from `handleInit` right after the existing `appendSubagentGuidelines(repoRoot)` call (line 231). The new section heading is exactly `## Context Budget and Subagent Orchestration Policy`. Guard with a heading-presence check (Pitfall 5 — skip if present, no duplicates) over `CLAUDE.md`, `GEMINI.md`, `AGENTS.md`. The section body must contain the 6 rules from RESEARCH Instruction Files: (1) the three outcomes `inline` / `context_pack_required` / `fresh_session_required` and the required action for each; (2) context-pack obligation — create a `.ai/context-packs/` pack before starting work, reference files by path, record omissions; (3) fresh-session handoff obligation — write `.ai/state/context-handoff.json` then stop (and note that this fixed well-known path IS the resume linkage a new session reads — D-16-16); (4) fan-out gate — only independent, non-sequential, disjoint-write-target tasks; one context pack per subagent; cap at `max_parallelism` default 3 (D-16-09, D-16-10, D-16-11); (5) sequential inline fallback when the runtime cannot spawn subagents (D-16-13); (6) parent join obligation — parent owns reconciliation, do not advance the ledger until all subagent results are received and verified (D-16-12). This section supersedes/extends the Phase 14 subagent section per Open Question 1; do not delete the Phase 14 section — only add the new one when absent.

    Make no change to `handleDoctor` or `runAndReport` — they pick up the new check IDs automatically via `runStrictChecks` (RESEARCH Integration Points). Do not add a new CLI command. Do not spawn subprocesses.
  </action>
  <verify>
    <automated>D=$(mktemp -d); ( cd "$D" && node "$OLDPWD/bin/adp.js" init >/dev/null 2>&1; node -e "const c=require('$D/.ai/state/context-policy.json'); process.exit(c.inline_threshold_bytes===50000&&c.max_parallelism===3?0:1)" && grep -q "## Context Budget and Subagent Orchestration Policy" "$D/CLAUDE.md" && echo ok )</automated>
  </verify>
  <acceptance_criteria>
    - `bin/adp.js` `handleInit` writes `.ai/state/context-policy.json` only when absent, with `inline_threshold_bytes: 50000`, `pack_threshold_bytes: 200000`, `max_parallelism: 3`, empty `stage_overrides`, and the five `budget_inputs` booleans
    - Re-running `adp init` does not overwrite an existing `context-policy.json` (logs a skip)
    - `bin/adp.js` defines `appendContextPolicyGuidelines` and calls it from `handleInit`
    - After `adp init`, each of `CLAUDE.md`/`GEMINI.md`/`AGENTS.md` contains exactly one `## Context Budget and Subagent Orchestration Policy` heading
    - The appended section names all three outcomes, the context-pack obligation, the fresh-session handoff obligation (writing the fixed `.ai/state/context-handoff.json` path as the resume linkage — D-16-16), the fan-out gate with max_parallelism 3, the sequential inline fallback, and the parent join obligation
    - No new CLI command added; `handleDoctor`/`runAndReport` unchanged
  </acceptance_criteria>
  <done>adp init idempotently creates the policy config and appends the non-duplicating context-policy instruction section; doctor wiring is inherited.</done>
</task>

<task type="auto">
  <name>Task 3: Extend test-init-checks.js and test-cli.js for fail-closed policy/pack/handoff coverage</name>
  <files>validators/scripts/test-init-checks.js, validators/scripts/test-cli.js</files>
  <read_first>
    - validators/scripts/test-init-checks.js (existing runStrictChecks tests, temp-dir fixture pattern, assert helpers, summary+exit)
    - validators/scripts/test-cli.js (existing adp init / adp doctor integration tests, how it asserts exit codes and stdout, temp-dir scaffolding)
    - lib/init-checks.js (the new check IDs from Task 1)
    - bin/adp.js (the init policy-config creation and instruction section from Task 2)
    - .planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-RESEARCH.md (Phase Requirements -> Test Map rows mapping to test-init-checks.js and test-cli.js: CTX-05 policy.config.schema fail, CTX-05 adp doctor exits 1 on missing required field)
  </read_first>
  <action>
    Extend `test-init-checks.js` with a section that builds a temp repo and asserts: (a) an out-of-range policy config (`inline_threshold_bytes >= pack_threshold_bytes`) produces a `policy.config.schema` result with `passed: false` that lands in `report.failures` (CTX-05); (b) an absent policy config yields `policy.config.exists` with `required: false` and does not appear in `report.failures` (warn-only); (c) a context pack missing a required field produces a `context.packs.schema` failure; (d) two packs in the same `subagent_fanout.group_id` with overlapping `write_targets` and empty `coordination_note` produce a `context.packs.fanout.conflicts` failure (D-16-19); (e) an instruction file lacking the new heading produces an `instructions.contextPolicySection` failure; (f) when a `.ai/state/context-handoff.json` exists at the fixed well-known path with a `resume_stage` not in the flow's known stage IDs, `handoff.schema` produces `passed: false` (D-16-15, D-16-16). Use the file's existing temp-dir + assert helpers and feed the same `passed`/`failed` counters; do not change the summary/exit lines.

    Extend `test-cli.js` with an integration section that: (a) runs `adp init` in a temp repo and asserts `.ai/state/context-policy.json` exists and the `## Context Budget and Subagent Orchestration Policy` heading was appended exactly once to CLAUDE.md; (b) re-runs `adp init` and asserts no duplicate heading and the policy config is unchanged (idempotency); (c) writes a malformed context pack (missing required field) under `.ai/context-packs/` then runs `adp doctor` and asserts a non-zero exit code (CTX-05 fail-closed via doctor). Follow the file's existing exit-code/stdout assertion conventions.
  </action>
  <verify>
    <automated>node validators/scripts/test-init-checks.js && node validators/scripts/test-cli.js && echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `test-init-checks.js` asserts an out-of-range policy config fails `policy.config.schema` and lands in `report.failures`
    - `test-init-checks.js` asserts an absent policy config is warn-only (not in failures)
    - `test-init-checks.js` asserts a missing-required-field pack fails `context.packs.schema` and a fan-out overlap fails `context.packs.fanout.conflicts`
    - `test-init-checks.js` asserts a handoff at the fixed `.ai/state/context-handoff.json` path with an unknown `resume_stage` fails `handoff.schema` (D-16-16)
    - `test-cli.js` asserts `adp init` creates the policy config and appends the heading exactly once, is idempotent on re-run, and `adp doctor` exits non-zero on a malformed pack
    - `node validators/scripts/test-init-checks.js` and `node validators/scripts/test-cli.js` both exit 0
  </acceptance_criteria>
  <done>Both extended suites prove the policy layer is detected by strict init and enforced fail-closed through adp doctor, and they pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.ai/` artifacts → runStrictChecks | Policy config, context packs, and handoff files read during doctor/init are untrusted structured input |
| init writes → user instruction files | `adp init` appends to CLAUDE.md/GEMINI.md/AGENTS.md and writes under `.ai/state/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Denial | runStrictChecks reading malformed policy/pack/handoff JSON | mitigate | Every read/parse wrapped in try/catch; surfaced as `passed: false` check, never a thrown exception that aborts doctor/init |
| T-16-08 | Elevation of Privilege | unsafe fan-out orchestration passing validation | mitigate | `context.packs.fanout.conflicts` (required:true) fails closed on overlapping write_targets with no coordination_note (D-16-19) |
| T-16-09 | Tampering | adp init overwriting existing policy config or duplicating instruction sections | mitigate | Idempotent write-only-if-absent for the config; heading-presence guard before appending the instruction section (Pitfall 5) |
| T-16-10 | Spoofing | handoff with a resume_stage that does not exist in the flow | mitigate | `handoff.schema` validates `resume_stage` at the fixed `.ai/state/context-handoff.json` path against known flow stage IDs (D-16-15, D-16-16) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependencies (RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `node validators/scripts/test-init-checks.js` exits 0.
- `node validators/scripts/test-cli.js` exits 0.
- The `node -e` runStrictChecks smoke check listing required IDs exits 0.
- The `adp init` temp-repo verify command creates the config and appends the heading.
- `npm test` passes the full suite with no regressions.
</verification>

<success_criteria>
- runStrictChecks emits all new policy/pack/handoff/instruction check IDs and fails closed for unsafe orchestration (D-16-17, D-16-18, D-16-19).
- The fresh-session handoff path is linked via state metadata by convention through the fixed `.ai/state/context-handoff.json` path, satisfying the D-16-16 state-metadata clause (terminal/chat-output half covered by 16-02).
- adp init idempotently creates the conservative-default policy config and appends the context-policy instruction section without duplication (D-16-03, D-16-06).
- adp doctor blocks on malformed context packs through the existing runAndReport path (CTX-05).
- Extended init-checks and CLI suites cover the fail-closed behavior and pass.
- No new CLI command, subprocess, network, or LLM-as-judge logic introduced.
</success_criteria>

<output>
Create `.planning/phases/16-context-budget-gate-and-subagent-orchestration-policy/16-03-SUMMARY.md` when done.
</output>
