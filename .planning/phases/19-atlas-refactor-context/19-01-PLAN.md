---
phase: 19-atlas-refactor-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/contracts/artifact-map.json
  - .claude/skills/contracts/entities.schema.json
  - .claude/skills/contracts/gate-result.schema.json
  - lib/artifact-paths.js
  - validators/scripts/test-artifact-paths.js
autonomous: true
requirements: [RAOS-02, RAOS-03]
must_haves:
  truths:
    - "artifact-map.json matches Appendix D paths and aliases exactly."
    - "entities.schema.json defines valid JSON schemas for claims, locks/leases, signals, and flow-state."
    - "gate-result.schema.json conforms to Appendix C schema definition exactly."
    - "lib/artifact-paths.js resolves canonical, feature, and staging paths without any hardcoded path strings."
    - "Unknown alias keys queried via artifact-paths throw a descriptive error."
    - "node validators/scripts/test-artifact-paths.js exits 0"
  artifacts:
    - path: ".claude/skills/contracts/artifact-map.json"
      provides: "Canonical path registry and template alias mappings"
    - path: ".claude/skills/contracts/entities.schema.json"
      provides: "Durable entity schemas for claims, leases, signals, and state"
    - path: ".claude/skills/contracts/gate-result.schema.json"
      provides: "Standard output envelope for verification and judgment gates"
    - path: "lib/artifact-paths.js"
      provides: "Runtime path resolution module with zero hardcoded paths"
      exports: ["resolve", "resolvePath", "getCanonical"]
    - path: "validators/scripts/test-artifact-paths.js"
      provides: "Unit test suite for artifact path resolution and schema compliance"
  key_links: []
---

<objective>
Build the shared Schema Contracts Foundation including the canonical path registry, durable entity schemas, standard gate result schemas, and the runtime path resolution module. This forms Wave 1 of the ATLAS Refactor Phase.

All subsequent slices in the ATLAS Refactor will import and depend on these schema contracts and the runtime path resolution module, eliminating hardcoded path strings and enforcing architectural consistency.
</objective>

<execution_context>
@.agents/skills/gsd-execute-phase/workflows/execute-plan.md
@.agents/skills/gsd-execute-phase/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-atlas-refactor-context/19-RESEARCH.md
@docs/prd-v4.2.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create contracts/artifact-map.json</name>
  <files>.claude/skills/contracts/artifact-map.json</files>
  <read_first>
    - docs/prd-v4.2.md (Appendix D: Artifact Map Reference)
  </read_first>
  <behavior>
    - Test: Contains valid JSON defining 'version', 'canonical', 'feature_artifacts', and 'staging_outputs' keys.
    - Test: Mappings for all 11 canonical items, 3 feature artifacts, and 5 staging outputs exactly match the PRD.
  </behavior>
  <action>
    Create `.claude/skills/contracts/artifact-map.json` and populate it with the schema:
    ```json
    {
      "version": "1.0",
      "canonical": {
        "flow_state": ".ai/state/flow-state.json",
        "claims_dir": ".ai/claims",
        "locks_dir": ".ai/locks",
        "signals_file": ".ai/signals/current-period.jsonl",
        "staging_dir": ".ai/staging",
        "reviews_dir": ".ai/reviews",
        "sessions_dir": ".ai/sessions",
        "memory_dir": ".ai/memory",
        "context_file": "CONTEXT.md",
        "adr_dir": "docs/adr",
        "feature_root": "specs"
      },
      "feature_artifacts": {
        "spec": "specs/{feature_slug}/spec.md",
        "plan": "specs/{feature_slug}/plan.md",
        "tasks": "specs/{feature_slug}/tasks.md"
      },
      "staging_outputs": {
        "matt_to_prd": ".ai/staging/{run_id}/matt-to-prd.md",
        "matt_to_issues": ".ai/staging/{run_id}/issue-candidates.md",
        "matt_handoff": ".ai/staging/{run_id}/handoff-draft.md",
        "codex_review": ".ai/reviews/{run_id}/codex-review.md",
        "gstack_review": ".ai/reviews/{run_id}/gstack-review.md"
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude/skills/contracts/artifact-map.json', 'utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/skills/contracts/artifact-map.json`.
    - JSON matches the reference from Appendix D exactly.
  </acceptance_criteria>
  <done>artifact-map.json created and validated as correct JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create contracts/entities.schema.json</name>
  <files>.claude/skills/contracts/entities.schema.json</files>
  <read_first>
    - docs/prd-v4.2.md (Section 3: Durable Flow State, and Appendix D references)
  </read_first>
  <behavior>
    - Test: Defines schemas for 'claim', 'lease', 'signal', and 'flow_state'.
    - Test: flow_state schema includes run_id, risk_profile, work_mode, attempt, completed_steps, and locks.
  </behavior>
  <action>
    Create `.claude/skills/contracts/entities.schema.json`. Define JSON schemas for all entity shapes to facilitate runtime validations.
    ```json
    {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ATLAS Entities Schema",
      "type": "object",
      "definitions": {
        "claim": {
          "type": "object",
          "properties": {
            "owner": { "type": "string" },
            "task": { "type": "string" },
            "profile": { "type": "string", "enum": ["FAST", "STANDARD", "FULL"] },
            "write_scope": { "type": "array", "items": { "type": "string" } },
            "started_at": { "type": "string", "format": "date-time" },
            "status": { "type": "string" }
          },
          "required": ["owner", "task", "profile", "write_scope", "started_at", "status"]
        },
        "lease": {
          "type": "object",
          "properties": {
            "owner": { "type": "string" },
            "pid": { "type": "integer" },
            "acquired_at": { "type": "string", "format": "date-time" },
            "stale_lock_cap_seconds": { "type": "integer" },
            "purpose": { "type": ["string", "null"] }
          },
          "required": ["owner", "pid", "acquired_at", "stale_lock_cap_seconds"]
        },
        "signal": {
          "type": "object",
          "properties": {
            "type": { "type": "string" },
            "value": { "type": ["string", "number", "boolean", "object"] },
            "reason": { "type": "string" },
            "timestamp": { "type": "string", "format": "date-time" }
          },
          "required": ["type", "value", "reason", "timestamp"]
        },
        "flow_state": {
          "type": "object",
          "properties": {
            "schema_version": { "type": "string", "const": "2.0" },
            "run_id": { "type": "string" },
            "feature_slug": { "type": "string" },
            "risk_profile": { "type": "string", "enum": ["FAST", "STANDARD", "FULL"] },
            "work_mode": { "type": "string", "enum": ["FEATURE", "BUGFIX", "PROTOTYPE", "REFACTOR", "DOCS"] },
            "stage": { "type": "string", "enum": ["align", "trace", "lay", "act", "settle"] },
            "status": { "type": "string", "enum": ["pending", "running", "done", "blocked", "needs_revision"] },
            "attempt": { "type": "integer", "minimum": 1 },
            "last_verified_commit": { "type": "string" },
            "completed_steps": { "type": "array", "items": { "type": "string" } },
            "pending_step": { "type": "string" },
            "locks": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "file": { "type": "string" },
                  "acquired_at": { "type": "string", "format": "date-time" }
                },
                "required": ["file", "acquired_at"]
              }
            },
            "signals": { "type": "array", "items": { "$ref": "#/definitions/signal" } },
            "last_gate": { "type": ["string", "null"] },
            "last_gate_status": { "type": ["string", "null"], "enum": ["PASS", "FAIL", "BLOCKED", "NEEDS_HUMAN_REVIEW"] },
            "consecutive_failures": { "type": "integer", "minimum": 0 },
            "retry_count": { "type": "integer", "minimum": 0 },
            "verified_artifacts": { "type": "array", "items": { "type": "string" } }
          },
          "required": [
            "schema_version",
            "run_id",
            "feature_slug",
            "risk_profile",
            "work_mode",
            "stage",
            "status",
            "attempt",
            "completed_steps",
            "pending_step",
            "locks",
            "signals",
            "consecutive_failures",
            "retry_count",
            "verified_artifacts"
          ]
        }
      }
    }
    ```
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude/skills/contracts/entities.schema.json', 'utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/skills/contracts/entities.schema.json`.
    - Defines schemas for claim, lease, signal, and flow_state.
  </acceptance_criteria>
  <done>entities.schema.json created and validated as correct JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create contracts/gate-result.schema.json</name>
  <files>.claude/skills/contracts/gate-result.schema.json</files>
  <read_first>
    - docs/prd-v4.2.md (Appendix C: Gate Result Schema)
  </read_first>
  <behavior>
    - Test: Schema matches Appendix C exact specification.
    - Test: Requires stage_id, status, blocking, warnings, and artifacts_produced.
    - Test: status must be one of PASS, FAIL, BLOCKED, NEEDS_HUMAN_REVIEW.
  </behavior>
  <action>
    Create `.claude/skills/contracts/gate-result.schema.json`. Define the JSON schema for validation gates.
    ```json
    {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Gate Result Envelope",
      "type": "object",
      "properties": {
        "stage_id": { "type": "string", "enum": ["align", "trace", "lay", "act", "settle"] },
        "status": { "type": "string", "enum": ["PASS", "FAIL", "BLOCKED", "NEEDS_HUMAN_REVIEW"] },
        "blocking": { "type": "array", "items": { "type": "string" } },
        "warnings": { "type": "array", "items": { "type": "string" } },
        "artifacts_produced": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["stage_id", "status", "blocking", "warnings", "artifacts_produced"]
    }
    ```
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude/skills/contracts/gate-result.schema.json', 'utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/skills/contracts/gate-result.schema.json`.
    - JSON matches Appendix C schema specification exactly.
  </acceptance_criteria>
  <done>gate-result.schema.json created and validated as correct JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement lib/artifact-paths.js</name>
  <files>lib/artifact-paths.js</files>
  <read_first>
    - .claude/skills/contracts/artifact-map.json (created in Task 1)
  </read_first>
  <behavior>
    - Test: Exports resolve(dottedKey, variables), resolvePath(dottedKey, variables), getCanonical(key).
    - Test: getCanonical('flow_state') returns '.ai/state/flow-state.json'.
    - Test: resolve('feature.spec', {feature_slug: 'abc'}) returns 'specs/abc/spec.md'.
    - Test: Unknown key throws with 'INVALID_KEY'.
  </behavior>
  <action>
    Implement `lib/artifact-paths.js` to parse `artifact-map.json` and resolve paths:
    ```javascript
    'use strict';

    const fs = require('fs');
    const path = require('path');

    // Locate artifact-map.json relative to repository root
    // Since we reside in lib/, root is one directory up
    const repoRoot = path.resolve(__dirname, '..');
    const mapPath = path.join(repoRoot, '.claude', 'skills', 'contracts', 'artifact-map.json');

    let map;
    try {
      map = JSON.parse(fs.readFileSync(mapPath, 'utf8'));
    } catch (err) {
      throw new Error(`Failed to load artifact-map.json from ${mapPath}: ${err.message}`);
    }

    function getCanonical(key) {
      if (!map.canonical || !(key in map.canonical)) {
        const err = new Error(`Canonical path key '${key}' not found in artifact map.`);
        err.code = 'INVALID_KEY';
        throw err;
      }
      return map.canonical[key];
    }

    function resolvePath(dottedKey, variables = {}) {
      const parts = dottedKey.split('.');
      if (parts.length === 1) {
        // Try canonical lookup
        try {
          return getCanonical(dottedKey);
        } catch (e) {
          if (e.code === 'INVALID_KEY') {
            const err = new Error(`Key '${dottedKey}' not found in artifact map.`);
            err.code = 'INVALID_KEY';
            throw err;
          }
          throw e;
        }
      }

      const [group, key] = parts;
      let template;

      if (group === 'feature' && map.feature_artifacts && (key in map.feature_artifacts)) {
        template = map.feature_artifacts[key];
      } else if (group === 'staging' && map.staging_outputs && (key in map.staging_outputs)) {
        template = map.staging_outputs[key];
      } else {
        const err = new Error(`Dotted key '${dottedKey}' does not match any registered templates.`);
        err.code = 'INVALID_KEY';
        throw err;
      }

      // Replace variables in format {var_name}
      return template.replace(/\{([a-zA-Z0-9_-]+)\}/g, (match, varName) => {
        if (!(varName in variables)) {
          throw new Error(`Missing template variable '${varName}' to resolve path '${dottedKey}'`);
        }
        return variables[varName];
      });
    }

    // resolve is an alias for resolvePath
    function resolve(dottedKey, variables = {}) {
      return resolvePath(dottedKey, variables);
    }

    module.exports = {
      resolve,
      resolvePath,
      getCanonical
    };
    ```
  </action>
  <verify>
    <automated>node -e "const p = require('./lib/artifact-paths'); console.log(p.getCanonical('flow_state')); console.log(p.resolve('feature.spec', {feature_slug: 'tst'}));"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/artifact-paths.js` loads the schema successfully.
    - Path lookup is correct and matches mapped values.
    - Throws code 'INVALID_KEY' for missing keys.
  </acceptance_criteria>
  <done>artifact-paths.js implemented and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement validators/scripts/test-artifact-paths.js unit tests</name>
  <files>validators/scripts/test-artifact-paths.js</files>
  <read_first>
    - lib/artifact-paths.js
    - validators/scripts/test-ownership-store.js (for test structure references)
  </read_first>
  <behavior>
    - Test: Greenfield resolve of canonical paths returns correct values.
    - Test: Greenfield resolve of feature paths with variables performs substitution.
    - Test: Missing variables for templates throws.
    - Test: Unknown path keys throw INVALID_KEY error.
  </behavior>
  <action>
    Create `validators/scripts/test-artifact-paths.js`. Use standard assert.
    ```javascript
    'use strict';

    const assert = require('assert');
    const path = require('path');
    const { resolve, resolvePath, getCanonical } = require('../../lib/artifact-paths');

    const tests = [];
    function addTest(name, fn) {
      tests.push({ name, fn });
    }

    addTest('getCanonical happy path', () => {
      const res = getCanonical('flow_state');
      assert.strictEqual(res, '.ai/state/flow-state.json');
    });

    addTest('getCanonical invalid key', () => {
      assert.throws(() => {
        getCanonical('nonexistent_key');
      }, (err) => {
        return err.code === 'INVALID_KEY';
      });
    });

    addTest('resolvePath canonical alias', () => {
      const res = resolvePath('flow_state');
      assert.strictEqual(res, '.ai/state/flow-state.json');
    });

    addTest('resolvePath feature template resolution', () => {
      const res = resolvePath('feature.spec', { feature_slug: 'my-feature' });
      assert.strictEqual(res, 'specs/my-feature/spec.md');
    });

    addTest('resolvePath staging template resolution', () => {
      const res = resolvePath('staging.matt_to_prd', { run_id: 'run_123' });
      assert.strictEqual(res, '.ai/staging/run_123/matt-to-prd.md');
    });

    addTest('resolvePath missing template variables throws', () => {
      assert.throws(() => {
        resolvePath('feature.spec', {});
      }, /Missing template variable 'feature_slug'/);
    });

    addTest('resolvePath invalid dotted alias throws', () => {
      assert.throws(() => {
        resolvePath('invalid.alias');
      }, (err) => {
        return err.code === 'INVALID_KEY';
      });
    });

    // Run tests
    let failed = false;
    for (const test of tests) {
      try {
        test.fn();
        console.log(`[PASS] ${test.name}`);
      } catch (err) {
        console.error(`[FAIL] ${test.name}:`, err);
        failed = true;
      }
    }

    if (failed) {
      process.exit(1);
    }
    ```
  </action>
  <verify>
    <automated>node validators/scripts/test-artifact-paths.js</automated>
  </verify>
  <acceptance_criteria>
    - running `node validators/scripts/test-artifact-paths.js` exits 0.
  </acceptance_criteria>
  <done>test-artifact-paths.js test suite written and executed successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Calling Code → lib/artifact-paths.js | Variable parameters (e.g. `feature_slug`, `run_id`) could be maliciously crafted with path traversals. Since resolving handles paths, any variables must be safe, or resolved paths must be handled safely downstream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | Path resolving input variables | mitigate | Ensure that resolvePath checks key structure, and missing template variables throw immediately instead of causing empty substitutions. Downstream components that write or read these paths will validate path resolution boundaries. |
</threat_model>

<verification>
- Run `node validators/scripts/test-artifact-paths.js` to ensure the library resolves aliases and template variables properly, and correctly throws for unknown aliases or missing variables.
</verification>

<success_criteria>
- Schema contract files `artifact-map.json`, `entities.schema.json`, and `gate-result.schema.json` exist under `.claude/skills/contracts/` and are valid JSON.
- `lib/artifact-paths.js` parses the artifact map and correctly implements `resolve`, `resolvePath`, and `getCanonical`.
- The test suite `validators/scripts/test-artifact-paths.js` executes and passes cleanly.
</success_criteria>

<output>
Creates plan 01 for the ATLAS refactor.
</output>
