---
phase: 21-memory-seeding-and-architecture-awareness-at-init
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .specify/templates/memory-project-summary-template.md
  - .specify/templates/memory-current-architecture-template.md
  - .specify/templates/memory-known-risks-template.md
  - .specify/templates/memory-decisions-template.md
  - .specify/templates/memory-verification-history-template.md
  - bin/adp.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Five memory template files exist under .specify/templates/ with parseable headings and guidance text."
    - "handleInit() in bin/adp.js seeds all five .ai/memory/ files when the directory is empty."
    - "Init is idempotent: running init twice does not overwrite existing memory files."
    - "Seeded files contain a <!-- Seeded by saf init --> comment so agents know to update them."
    - "npm test exits 0 after changes."
  artifacts:
    - path: ".specify/templates/memory-project-summary-template.md"
      provides: "Starter template for project summary memory file"
    - path: ".specify/templates/memory-current-architecture-template.md"
      provides: "Starter template for architecture memory file"
    - path: ".specify/templates/memory-known-risks-template.md"
      provides: "Starter template for known risks memory file"
    - path: ".specify/templates/memory-decisions-template.md"
      provides: "Starter template for decisions memory file"
    - path: ".specify/templates/memory-verification-history-template.md"
      provides: "Starter template for verification history memory file"
    - path: "bin/adp.js"
      provides: "Updated init handler that seeds .ai/memory/ files from templates"
  key_links: []
---

<objective>
Create five memory file templates and update `handleInit()` to seed `.ai/memory/` with starter files copied from these templates. This closes the gap where `saf init` creates the `.ai/memory/` directory but leaves it empty, causing agents to lack architecture context until the first Memory Handoff completes.

The seeding follows the exact same pattern used for `.ai/constitution.md` (check existence → copy template → fallback to inline default).
</objective>

<execution_context>
@.agents/skills/gsd-execute-phase/workflows/execute-plan.md
@.agents/skills/gsd-execute-phase/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-memory-seeding-and-architecture-awareness-at-init/21-CONTEXT.md
@bin/adp.js
@.ai/constitution.md
@.specify/templates/constitution-template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create memory-project-summary-template.md</name>
  <files>.specify/templates/memory-project-summary-template.md</files>
  <read_first>
    - .ai/constitution.md (artifact contract, line 120)
    - docs/prd.md (Memory Handoff rules, lines 357-394)
  </read_first>
  <action>
    Create `.specify/templates/memory-project-summary-template.md`:
    ```markdown
    <!-- Seeded by saf init. Update during Memory Handoff (Step 5.5). -->
    # Project Summary

    ## Purpose

    _Describe what this project does and who it serves._

    ## Stack

    - **Language(s):** _e.g. Node.js 20, Python 3.12_
    - **Framework(s):** _e.g. Next.js, FastAPI_
    - **Package manager:** _e.g. npm, pnpm_

    ## Key Components

    _List the major modules, services, or packages and their responsibilities._

    ## Current State

    _Brief description of project maturity: greenfield, active development, legacy maintenance, etc._
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.specify/templates/memory-project-summary-template.md', 'utf8'); if (!c.includes('# Project Summary')) throw new Error('Missing heading'); if (!c.includes('Seeded by saf init')) throw new Error('Missing seed comment'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# Project Summary` heading and seed comment.
  </acceptance_criteria>
  <done>Template created.</done>
</task>

<task type="auto">
  <name>Task 2: Create memory-current-architecture-template.md</name>
  <files>.specify/templates/memory-current-architecture-template.md</files>
  <read_first>
    - .ai/memory/current-architecture.md (existing content in this repo)
    - .claude/skills/project-onboarding/ONBOARDING-template.md (architecture section format)
  </read_first>
  <action>
    Create `.specify/templates/memory-current-architecture-template.md`:
    ```markdown
    <!-- Seeded by saf init. Update during Memory Handoff (Step 5.5). -->
    # Current Architecture

    ## Directory Structure

    _Map the major source directories and their purposes._

    ## State Pointers

    _List configuration files, state files, and their roles._

    ## Data Flow

    _Describe how data moves through the system: entry points, processing, storage._

    ## Key Components

    _Name the core modules/services and their boundaries._

    ## External Dependencies

    _APIs, databases, third-party services this project integrates with._

    ## Architecture Decisions

    _Record significant architectural choices with date and rationale._
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.specify/templates/memory-current-architecture-template.md', 'utf8'); if (!c.includes('# Current Architecture')) throw new Error('Missing heading'); if (!c.includes('Seeded by saf init')) throw new Error('Missing seed comment'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# Current Architecture` heading, 6 sections, and seed comment.
  </acceptance_criteria>
  <done>Template created.</done>
</task>

<task type="auto">
  <name>Task 3: Create memory-known-risks-template.md</name>
  <files>.specify/templates/memory-known-risks-template.md</files>
  <action>
    Create `.specify/templates/memory-known-risks-template.md`:
    ```markdown
    <!-- Seeded by saf init. Update during Memory Handoff (Step 5.5). -->
    # Known Risks

    ## Active Risks

    _List risks that are currently relevant. Include date identified and affected areas._

    ## Mitigated Risks

    _Risks that have been addressed. Keep for historical context._

    ## Do-Not-Touch Zones

    _Files, modules, or patterns that are fragile or have hidden dependencies._
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.specify/templates/memory-known-risks-template.md', 'utf8'); if (!c.includes('# Known Risks')) throw new Error('Missing heading'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# Known Risks` heading and seed comment.
  </acceptance_criteria>
  <done>Template created.</done>
</task>

<task type="auto">
  <name>Task 4: Create memory-decisions-template.md</name>
  <files>.specify/templates/memory-decisions-template.md</files>
  <action>
    Create `.specify/templates/memory-decisions-template.md`:
    ```markdown
    <!-- Seeded by saf init. Update during Memory Handoff (Step 5.5). -->
    # Decisions

    _Record decisions that affect architecture, behavior, operations, or security._
    _Format: date, decision, rationale, affected files/modules._

    ## Architecture Decisions

    _None recorded yet._

    ## Behavior Decisions

    _None recorded yet._

    ## Security Decisions

    _None recorded yet._
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.specify/templates/memory-decisions-template.md', 'utf8'); if (!c.includes('# Decisions')) throw new Error('Missing heading'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# Decisions` heading and seed comment.
  </acceptance_criteria>
  <done>Template created.</done>
</task>

<task type="auto">
  <name>Task 5: Create memory-verification-history-template.md</name>
  <files>.specify/templates/memory-verification-history-template.md</files>
  <action>
    Create `.specify/templates/memory-verification-history-template.md`:
    ```markdown
    <!-- Seeded by saf init. Update during Memory Handoff (Step 5.5). -->
    # Verification History

    _Record verification results that future agents should know about._
    _Format: date, feature/change, verification method, result, notes._

    ## Verification Log

    _No verifications recorded yet._
    ```
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.specify/templates/memory-verification-history-template.md', 'utf8'); if (!c.includes('# Verification History')) throw new Error('Missing heading'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `# Verification History` heading and seed comment.
  </acceptance_criteria>
  <done>Template created.</done>
</task>

<task type="auto">
  <name>Task 6: Update handleInit() to seed .ai/memory/ files</name>
  <files>bin/adp.js</files>
  <read_first>
    - bin/adp.js (handleInit function, lines 94-309 — specifically the constitution copy pattern at lines 130-152)
  </read_first>
  <behavior>
    - After init, `.ai/memory/project-summary.md` exists with content from template.
    - After init, `.ai/memory/current-architecture.md` exists with content from template.
    - After init, `.ai/memory/known-risks.md` exists with content from template.
    - After init, `.ai/memory/decisions.md` exists with content from template.
    - After init, `.ai/memory/verification-history.md` exists with content from template.
    - Running init twice does not overwrite existing memory files.
    - Each seeded file contains the `<!-- Seeded by saf init -->` marker.
  </behavior>
  <action>
    Add a new `seedMemoryFiles(repoRoot)` function to `bin/adp.js`, called from `handleInit()` after the directory creation loop (line 128) and before the constitution copy (line 130).

    The function follows the constitution template copy pattern:
    ```javascript
    function seedMemoryFiles(repoRoot) {
      const memoryFiles = [
        { target: '.ai/memory/project-summary.md', template: 'memory-project-summary-template.md' },
        { target: '.ai/memory/current-architecture.md', template: 'memory-current-architecture-template.md' },
        { target: '.ai/memory/known-risks.md', template: 'memory-known-risks-template.md' },
        { target: '.ai/memory/decisions.md', template: 'memory-decisions-template.md' },
        { target: '.ai/memory/verification-history.md', template: 'memory-verification-history-template.md' }
      ];

      for (const { target, template } of memoryFiles) {
        const targetPath = path.join(repoRoot, target);
        const templatePath = path.join(packageRoot, '.specify/templates', template);

        if (!fs.existsSync(targetPath)) {
          if (fs.existsSync(templatePath)) {
            fs.copyFileSync(templatePath, targetPath);
            console.log(`[init] Created ${target} (copied from template)`);
          } else {
            // Fallback: create minimal placeholder
            fs.writeFileSync(targetPath, `<!-- Seeded by saf init. Update during Memory Handoff. -->\n# ${path.basename(target, '.md').split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ')}\n\n_No content yet. Update during Memory Handoff (Step 5.5)._\n`, 'utf8');
            console.log(`[init] Created ${target} (default written)`);
          }
        } else {
          console.log(`[init] ${target} already exists, skipping.`);
        }
      }
    }
    ```

    Call `seedMemoryFiles(repoRoot);` from `handleInit()` after line 128 (end of directory creation loop).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `handleInit()` calls `seedMemoryFiles()`.
    - All 5 memory files are created on first init.
    - Existing files are not overwritten on re-init.
    - npm test passes.
  </acceptance_criteria>
  <done>Init now seeds memory files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template → Target FS | Templates are copied from package into target project. Templates are static markdown — no code execution risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.1-01 | Information Disclosure | Memory seed files | accept | Seeded files contain only placeholder text with guidance. No secrets or project-specific information is included. |
</threat_model>

<verification>
- Run `npm test` to verify all existing tests still pass.
- Run `node bin/adp.js init` in a fresh temp directory and verify all 5 `.ai/memory/` files exist.
- Run init a second time and verify files are not overwritten.
</verification>

<success_criteria>
- Five memory templates exist under `.specify/templates/`.
- `handleInit()` seeds `.ai/memory/` with these templates on first run.
- Init idempotency is preserved — re-run does not overwrite.
- `npm test` passes cleanly.
</success_criteria>
