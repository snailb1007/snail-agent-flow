---
quick_id: 260527-152
description: update readme — sync with v0.4.0.0
status: complete
date: 2026-05-26
commit: c58dd92
---

# Quick Task 260527-152: update readme

## Scope (user-locked)

"Sync with v0.4.0.0" — review README.md against actual code/CLI/state and fix staleness. No restructuring, no new sections.

## Changes applied

| ID | Section | Change |
|---|---|---|
| S2 | Verification block | Added `npm run test:init-checks` (script exists in `package.json`; missing from README) |
| S3 | CLI table — `init` row | Expanded to cover flow YAML copy (`.ai/flows/rough-project-flow.yaml`), ledger init (`.ai/state/flow-ledger.json`), and `project-flow` skill stub generation in both `.agents/skills/` and `.claude/skills/` (verified against `bin/adp.js:162-226`) |
| S4 | Project Structure table | Added `lib/` row (shipped runtime modules — init-checks, flow engine/ledger, parsers, tool-validator) and `.ai/flows/` row |

## Changes rejected

| ID | Original plan claim | Verified reality | Decision |
|---|---|---|---|
| S1 | `docs/runbooks/failure-modes.md` does not exist; README link is broken | `docs/runbooks/failure-modes.md` **exists** (3370 bytes). README link is correct. | Skipped — executor caught the inversion; applying it would have broken a working link. |

## Files

- Modified: `README.md` (+9 / −6)
- Commit: `c58dd92`
- Branch: `015-improve-ai-spawn`

## Verification

- `ls docs/runbooks/failure-modes.md` → present
- `grep test:init-checks package.json` → present
- `ls lib/` → 6 modules (init-checks, flow-engine, flow-ledger, skill-md-parser, yaml-parser, tool-validator)
- `ls .ai/flows/` → `rough-project-flow.yaml` present
- `grep -n flowDestPath bin/adp.js` → line 162 confirms init copies the flow template
